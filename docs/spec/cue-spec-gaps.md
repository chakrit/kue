# CUE spec gaps

Areas where the CUE language **spec itself is incomplete, ambiguous, or silent**, so
neither the written spec nor the reference `cue` binary's behavior is authoritative. Here
Kue makes a principled, mathematically-grounded choice and records *why*.

This is distinct from [`cue-divergences.md`](cue-divergences.md): a *divergence* is where
the correct behavior IS clear and the `cue` binary is wrong; a *spec gap* is where the spec
does NOT pin the behavior at all, so the binary's behavior is just an implementation
artifact.

A spec gap is worth recording **even when Kue matches the `cue` binary** — matching an
artifact is not the same as honoring a spec mandate. Such a match is lower-confidence (if
`cue` changes, the "oracle" moves under us), and the principled basis for the choice
belongs on the record, not buried in a fixture. The continuous slice loop appends an entry
whenever a slice's oracle-check surfaces behavior the binary exhibits but the spec does not
mandate (see `CLAUDE.md` → "Continuous slice loop" and
[`../guides/slice-loop.md`](../guides/slice-loop.md)).

## How to record an entry

When a slice grounds a fix against `cue` behavior, ask: **is this behavior spec-grounded,
or just what the binary does?** If the latter:

1. Check the CUE language spec (and, where useful, the upstream issue tracker) to confirm
   the spec is genuinely silent or ambiguous on the point — not that you misread it.
2. Record Kue's chosen behavior and its principled basis (the repo's philosophy: precise,
   total, illegal-states-unrepresentable, mathematically defensible — see `CLAUDE.md` →
   Project and the working agreement).
3. Note whether Kue's choice MATCHES or DIFFERS from the `cue` binary, and the confidence.
4. The fixture/pin is the executable record; this table is the human-readable index.

## Entry format

| Topic | `cue` ver | Spec status | `cue` binary behavior | Kue's choice + basis | Matches cue? | Fixture |
|-------|-----------|-------------|-----------------------|----------------------|--------------|---------|

## Recorded spec gaps

| Topic | `cue` ver | Spec status | `cue` binary behavior | Kue's choice + basis | Matches cue? | Fixture |
|-------|-----------|-------------|-----------------------|----------------------|--------------|---------|
| **B-1 (2026-07-11)** — `unsupportedBuiltin` marker scope: a RECOGNIZED-real-but-deferred leaf vs a NONEXISTENT leaf name | v0.16.1 | Silent, same diagnostic-surface family as TL-1 (the VERDICT for an unimplemented builtin is decidable — bottom, a call to a non-function is not a value — but the MESSAGE is unpinned). cue has NO "unsupported builtin" concept at all: it never distinguishes "this real function isn't built yet" from "this leaf doesn't exist". | Every unimplemented OR nonexistent leaf → the SAME `cannot call non-function <name> (type _\|_)` bottom (probed v0.16.1: `strconv.Itoa`, `strconv.CanBackquote`, `regexp.FindString`, `regexp.Split` all non-function errors). A leaf cue DOES implement returns a real VALUE (`strconv.FormatFloat(1.5,…)="1.5"`, `strconv.Quote("hi")`, `regexp.FindNamedSubmatch(…)` → struct, `regexp.QuoteMeta`, `regexp.Valid`). | **Marker = a POSITIVE recognition claim ("kue recognizes this real `cue` function but defers computing it"), emitted ONLY from an EXPLICIT dispatch arm that names the leaf; the catch-all defaults to bare `.bottom` (default-deny the claim).** One `unsupportedOrBottom` combinator (sibling of `unresolvedOrBottom`, differing only in the all-concrete branch: marker vs bare bottom) backs every explicit deferred-leaf arm — replacing the two byte-identical inline blocks (strconv, regexp). Explicit-arm sets: strconv `FormatFloat`/`ParseFloat` (Float model) + `Quote`/`Unquote`/`QuoteToASCII` (Go Unicode `IsPrint` table); regexp `FindNamedSubmatch`/`FindAllNamedSubmatch` (named-capture parser, RX-1a). A nonexistent leaf (`Itoa`, `FindString`, `Split`) has NO arm → catch-all → bare bottom, matching cue's verdict. Basis: the catch-all cannot tell a deferred-but-real leaf from a nonexistent one (kue has no cue-function registry), so asserting "unsupported" there is an unsubstantiable claim — general-coding default-deny + illegal-states. Corrects the prior blunt "mark every concrete-unmatched leaf" which mislabeled nonexistent `Itoa`/`FindString` (their own comments admitted "not a callable function in cue") as "recognized". | verdict yes (nonexistent → bottom, matching cue; a recognized-but-deferred leaf also bottoms where cue returns a value — kue can't compute Float/Unicode/named-capture forms, a deferred capability, kue-does-less-than-cue, NOT a cue bug, so recorded here not in `cue-divergences.md`); MESSAGE no (kue adds `unsupportedBuiltin` where cue gives a plain non-function error, and bottoms where cue computes a value) | pins `StrconvTests` `itoa_nonexistent_is_bottom` (nonexistent→bottom), `parsefloat_deferred`/`quote_deferred`/`quote_render_message` (real→marker + CLI text); `BuiltinTests` `regexp_findstring_nonexistent_is_bottom`, `regexp_findnamedsubmatch_is_unsupported`, `unknown_leaf_each_family_concrete_args_is_bottom` |
| **STDLIB-D (2026-07-10)** — Parse-error MESSAGE TEXT for a missing declaration separator | v0.16.1 | The GRAMMAR is pinned (SourceFile = `[PackageClause] {ImportDecl} {Declaration}`; declarations are comma/newline separated — imports precede declarations); the exact diagnostic WORDING is not spec-mandated. | Context-dependent: `a: 1 b: 2` → `missing ',' in struct literal`; `import "x"`/`foo "bar"` after a field → `expected label or ':', found 'STRING'` (different lexer path). Both are rejections. | Enforce the grammar via one general rule: a newline/`,`/`;` must separate declarations (`fieldSeparator` in `parseFieldsUntil`), and a trailing binary operator only continues an expression when it sits at line END (`skipSameLineTrivia` — CUE's implicit-comma-at-newline). Kue emits ONE uniform message, `missing ',' in struct literal`, for every missing-separator case (incl. late import), rather than replicating cue's path-dependent wording. Basis: spec pins rejection, not text; a single clear message is more total than mirroring a lexer artifact. | rejection: yes; exact text: no (uniform `missing ','`) | `testdata/wild/import-after-decl/`; `Kue/Tests/ParseTests.lean` § Import placement + field separators |
| **RATIFIED (2026-06-20)** — Unreferenced bottom def in an imported package | v0.16.1 | Silent. The recursive-bottom rule governs unification *results*; it does not say a package containing a standalone-bottom unreferenced definition is itself bottom. Spec re-checked: genuinely silent (the only mandate is that a *referenced* `_\|_` propagates, which Kue honors). | Tolerates — `main` exports clean even when an unreferenced `dep.#Probe` is `_\|_`. | **RATIFIED: tolerate, on an OPERATIONAL-LAZINESS basis** (real packages carry unused, not-fully-resolved defs; forcing every imported def is wasteful and would surface defects in code the consumer never touches). The `FieldClass.importBinding` marker keeps the whole imported package shallow by construction, so the A2-followup deep-bottom recurse never fires on it. The lattice-purist reading (a bottom value is bottom whether selected-or-not) is acknowledged and DECLINED: CUE's value model is demand-driven — a field's value is realized when selected, so an unselected def contributes nothing. Smell (reference-location-dependent: an in-file conflict surfaces, an imported one does not) is recorded and accepted — it is the demand-driven model's intended consequence, not an inconsistency. cue agrees, but the basis is operational, not "because cue does". | yes (re-verified — `main` exports `{"out":{"name":"ok"}}`) | `testdata/modules/unreferenced_import_conflict` (runs via `check-fixtures.sh`); the recategorized field-less RX-2b sub-case pinned by `LatticeTests` `rx2b_label_pattern_invalid_bottoms` |
| **RATIFIED (2026-06-20)** — Un-narrowed struct-arm disjunction with no unique default (`{…} \| {…}`) | v0.16.1 | Silent on open-vs-error for an unresolved struct-shaped disjunction. Spec re-checked: the disjunction/default rules define narrowing and default *selection* at concretization, but say nothing that forces an un-narrowed multi-arm struct join to a single value or an error. Genuinely silent. | Keeps it open as the two-arm join (`{a:int} \| {b:string}`). | **RATIFIED: keep open.** Lattice basis: a join `⊔` with no unique default IS the join — it is a fully-formed lattice value, not an incompleteness and not an error. Collapsing or erroring would over-commit (discard a live arm a later meet can still select). Verified the value is **meet-identity** against `.top` (`disj_struct_arms_no_default_is_meet_identity`), confirming it is a settled value, not an intermediate awaiting resolution. (Correction to the prior note: it is NOT rendered as `incomplete` — it is the open disjunction value itself.) cue agrees. | yes | pins `StructTests` `disj_struct_arms_no_default_stays_open` + `_is_meet_identity`; existing disjunction fixtures |
| **RATIFIED (2026-06-20)** — Output field order of a struct meet | v0.16.1 | No mandated ordering. Spec re-checked + confirmed: structs are unordered sets of fields ("field order … irrelevant" for equality; the unification result "has all fields of both" with no ordering clause); output rendering order is explicitly implementation-defined. Genuinely silent. | **CORRECTED (the prior "first-seen-across-conjuncts → `a,b`" was a wrong characterization of cue):** cue's cross-conjunct order is a COMPLEX internal-graph artifact, not a single rule. Re-probed v0.16.1: separate one-field literals come out *sorted* (`{b}&{a}` → `a,b`; `{z}&{a}&{m}` → `a,m,z`; `{b,d}&{a,c}` → `a,b,c,d`), but a def-ref meet does NOT sort (`#Def:{kind,zfield} & {own,afield}` → `kind, own, afield, zfield` — interleaved by introduction). It preserves declaration order WITHIN a single struct literal (`{z,a,m}` → `z,a,m`). So cue's order is an undocumented arc/graph ordering, definitively not spec-mandated and not cleanly reproducible. | **RATIFIED: declaration / first-seen-across-conjuncts** (`{b}&{a}` → `b,a`; left-conjunct fields first, in source order). Basis: the source/declaration order is the most informative and least surprising rendering — it mirrors the program text and cue's own *intra*-struct order, and it is total + deterministic (one trivial rule, no comparator over arbitrary label types, no internal-graph reconstruction). Kue deliberately does NOT chase cue's cross-conjunct order — that would gate on reverse-engineering a `cue` presentation artifact the spec does not mandate (and which is not even self-consistent across meet shapes). Supersedes plan item #4 ("field-ordering parity #3"): the parity work is DECLINED, not deferred-pending. | partial — intra-struct yes (both declaration-order); cross-conjunct Kue (declaration, `b,a`) vs cue (graph-order, often sorted, `a,b`) | pins `StructTests` `meet_struct_field_order_is_declaration_order`, `meet_disjoint_regular_structs`, `LatticeTests` `mergeStructN_struct_tail_reverses_field_order` |
| Invalid pattern label with no field to match (RX-2b) | v0.16.1 | Silent on WHEN a pattern constraint must be validated. The spec mandates RE2 SYNTAX for the regex (`a(` is ill-formed), but says nothing about whether an UNEXERCISED pattern constraint — one no concrete field is ever tested against — is validated at parse time or lazily on first match. | Tolerates `{[=~"a("]: int}` → `{}` (lazy: validates only when a field's label is tested — `{[=~"a("]: int, k: 1}` DOES error, agreeing with Kue). The behavior is reference-location-dependent (field present ⇒ errors; field-less ⇒ silently kept). | Bottom EAGERLY the moment the concrete pattern literal is present (`_\|_`), independent of whether a field matches it — same operational-laziness family as the import-laziness gap (row 1) and SC-2b: cue's tolerance is an eval-strategy artifact, not a spec mandate. Lattice basis: a struct carrying a malformed constraint is ill-formed regardless of what is later added by meet (illegal-states-unrepresentable — don't defer a parse-time defect). NB: with a field present, cue and Kue AGREE (both error) — the divergence is confined to the field-less case. | partial — field-present yes (both error); field-less Kue (`_\|_`) ≠ cue (`{}`) | `definitions/regex_invalid_pattern_label` (field-present, both agree); field-less pinned in `LatticeTests` `rx2b_label_pattern_invalid_bottoms` |
| Structural-cycle SCOPE + display (D#2a) | v0.16.1 | The spec mandates dynamic detection ("Implementations should be able to detect such structural cycles dynamically"; "a node is valid if any of its conjuncts is not cyclic") but is silent on (a) whether detection is def-only or also regular-field, (b) recursion through a deferred list tail, and (c) the error's display form. | Errors `#L.next: structural cycle` on def AND regular self-ref; `[...#L]` recursion terminates (`[]`, the tail defers); reports the cycle at the field path. | Detect struct-body RE-ENTRANCY regardless of definition-ness (a structural cycle is "a value whose evaluation requires its own evaluation" — def-ness is irrelevant; lattice-principled, illegal-states). EXCLUDE list-tail recursion (an open list tail is a deferred constraint that yields `[]`, the standard recursive-tree idiom — not a cycle). Display: the cyclic field carries a `.structuralCycle` bottom (`next: _\|_` in `eval`, Kue's standard nested-bottom convention — see the "residual pattern in eval output" divergence); `export` bottoms (value verdict). The VALUE verdict (error vs terminated) matches cue on every probe (#1/#3/#4/#5 + list); only the eval *display* of the error differs (cue prints `… structural cycle` text + source span, Kue shows nested `_\|_`). | yes (value verdict — def, regular, mutual, finite-deep, list-tail all match cue); display differs (Kue nested `_\|_` vs cue error text) | `refs/structural_cycle_struct`, `refs/structural_cycle_mutual`; pins in `EvalTests` `structural_cycle_*` / `*_unchanged` / `finite_deep_*` / `recursive_list_tail_*` |
| Eval-display of a resolvable-default disjunction (D#2b / SC-3) | v0.16.1 | Silent. `eval` is a presentation mode, not a value; the spec defines value semantics (default *selection* at concretization), not how an intermediate disjunction-with-default is rendered. | `eval` display-collapses to the default: `*1 \| 2` → `1`, `*1 \| *1 \| 2` → `1`, the terminating `#List \| *null`.tail → `null`. The underlying VALUE is unchanged (the non-default arm is still live — `b: a & 2` yields `2`, not `1 & 2 = _\|_`). | Show the full deduped disjunction-with-default in `eval` (`*1 \| 2`; `#List \| *null`.tail → `{…} \| *null`), NOT cue's collapsed default. Basis: (1) collapsing into the value is UNSOUND — it loses the live non-default arm a later meet needs (`b: a & 2 = 2` requires the `2` arm); cue itself keeps the value (only its *display* collapses), so a value-rewrite would diverge. (2) Showing the marked disjunction is MORE informative and illegal-states-honest (the value genuinely IS a disjunction-with-default). `normalizeEvaluatedDisj` prunes `containsBottom` arms (drops the `.structuralCycle` arm, SC-3 flatten/dedup) but never collapses a multi-live-arm default. **Scope (AD2-1, 2026-06-21):** this "keep marked" display applies ONLY to MULTI-arm live defaults, where the mark IS load-bearing (it selects among live arms a later meet can still pick). A LONE default (one live arm — direct or a residual from a collapsed larger disjunction) is NO LONGER part of this gap: its mark is vacuous (value-identical to the bare value in every onward meet), so BOTH normalizers now collapse it (`*v`-lone → `v`), matching cue's display. The VALUE verdict (`export`) matches cue on every D#2b probe. **All-default sub-case (2026-07-04 AFK probe):** a markerless×markerless disj-meet (`(1|2)&(1|2)`, or a direct `*1\|*2`) yields an ALL-default result — `withDefaultConvention` promotes each markerless operand's whole set to defaults, so the surviving arms are all `.default` (default-set = full set). Kue eval renders `*1 \| *2`; cue elides to `1 \| 2`. Same SC-3 keep-marked family; export is identical (both ambiguous). A "demote all-default → regular" fix is NOT globally sound — applied in the shared normalizer it breaks `(*1\|*2)\|*3` (where the inner all-default must collapse to regular so the outer `*3` wins, export `3`; a blanket demote would keep them and re-flatten wrong), so it stays a display gap, not forced. Guards: `EvalTests` `disj_meet_*`. | partial — VALUE verdict matches cue (every `export` byte-identical); eval *display* differs (Kue `*1 \| 2` / `{…} \| *null` vs cue `1` / `null`) — same family as `disjunctions/default_disjunction.expected` (Kue `*"prod" \| "dev"` vs cue `"prod"`) | `testdata/export/terminating_disj_{default,nonnull_default,cyclic_nondefault}` (value); pins `EvalTests` `terminating_disj_*` / `sc3_*`; `disjunctions/default_dedup.expected` (deduped eval form) |
| Incomplete comprehension-guard deferral (D#1b) | v0.16.1 | The spec mandates the `if` clause guard be of type `bool` (so a CONCRETE non-bool is a type error — D#1c, where cue+Kue AGREE, not a gap). It is SILENT on what a finalizing evaluator does when the guard is *incomplete* (not yet a concrete bool — an abstract type, an unresolved disjunction, a bound comparison): hold the comprehension, error, or drop. cue resolves this by EVAL MODE, which is a presentation choice, not a value-spec mandate. | Mode-split: `cue eval` HOLDS the comprehension residual (`if x {…}` stays in the output, an incomplete value); `cue export` ERRORS (`incomplete bool` / `unresolved disjunction … (type bool)`). Both treat a residual PRESENCE test `X != _|_` (X absent/incomplete) as not-satisfied ⇒ DROP, not hold. | DEFER an incomplete guard — keep the comprehension as a residual `.structComp`/`.comprehension`/`.listComprehension` node rather than silently dropping it to `{}`/`[]` (the pre-D#1b bug). Kue is finalizing-eval-style: its default `kue` output HOLDS incompletes (like a held `x: int`), so a held comprehension is consistent; `kue export` then surfaces it as an incompleteness (`incomplete value: bool`, via `Manifest`'s `.comprehension → incomplete` arm). Basis: an undecidable guard means the comprehension's contribution is genuinely not-yet-determined — deciding it as empty is unsound (it loses a body a later meet, concretizing the referenced field, would produce — exactly the force-path case `if port > 0 {pos}` that resolves once `port: 8080` lands). The residual PRESENCE-test shape (`X !=/== _|_`) is excluded from deferral and still DROPS, matching cue eval and preserving the pre-existing `classifyDefinedness` design. | yes (value verdict — held under eval, incomplete under export, both match cue; presence-test drop matches cue) — eval *display* of the held residual differs (Kue renders the resolved guard ref as `@d.i`, cue prints the source name; recorded in `cue-divergences.md` D#1b row) | `comprehensions/guard_incomplete_defers` (held); pins `PresenceTests` `classify_guard_*` / `guard_incomplete_*` |
| Comprehension `let`-clause value evaluation order (D#3) | v0.16.1 | Silent. The spec fixes the SCOPE of a `let` clause (*"The `for` and `let` clauses each define a new scope in which new values are bound to be available for the next clause"* — Kue honors this exactly: `let` = +1 frame, like `for`; `if` = +0) but says nothing about WHEN the bound value is evaluated or whether an unreferenced binding is forced. That is an eval-order artifact, not a value mandate. | Lazy on the unused-binding axis but eager-on-reference: an UNREFERENCED `let bad = div(1,0)` does NOT surface its bottom (`out: [{v:1},{v:2}]`); a REFERENCED one DOES (`{v: bad}` → division-by-zero error). cue is also lazy for an unreferenced unresolved REFERENCE error though (`let unused = someUndef` unused → ERRORS — see the D#3 row in `cue-divergences.md`; that single sub-case diverges). | Bind the `let` value into a one-slot frame (`loopFrame none _ name evaluated`, exactly like a `for` element), evaluating it in the pre-push `env` (its resolve-time scope, symmetric with the `for` source). The evaluated value carries no residual refIds, so the frame stays alignment-correct as `for` does — this is why a `for` AFTER a `let` still resolves earlier bindings (`letcomp_for_after_let`). An unreferenced binding's value sits unread in the frame, so its bottom never propagates (the `.refId`-on-select path is the only force) — matching cue for a value-level bottom. Basis: reuse the proven `for`-element frame machinery (minimal, total, illegal-states — no new lazy-thunk representation) rather than a bespoke lazy binder; a dead binding contributing nothing is lattice-correct. | yes for the value-level-bottom unreferenced case (`letcomp_unreferenced_bottom_drops`) and every referenced case; the single unreferenced-UNRESOLVED-REF sub-case diverges (cue errors, Kue tolerates — `cue-divergences.md` D#3 row) | `comprehensions/list_let_*`, `struct_let_basic`, `let_shadows_outer`; pins `ComprehensionTests` `letcomp_*` |
| `math.Pow`/`math.Sqrt` precision model + computed domain (BI-2, extended BI-2-residual) | v0.16.1 | Silent on numeric precision. The spec describes the `math` package as mirroring Go's `math`, but cue's `math.Pow` does NOT use Go's float64 `math.Pow` — it uses an apd decimal context (34 significant digits): `Pow(2, 0.5) = 1.414213562373095048801688724209698`, `Pow(3, -1) = 0.3333…3333` (34, padded). `math.Sqrt` uses a DIFFERENT library (float64): `Sqrt(2) = 1.4142135623730951` (17 digits), `Sqrt(100) = 1e+1`. A POSITIVE-INTEGER `Pow` exponent is exact arbitrary-precision multiplication (`Pow(0.1, 2) = 0.01`, unpadded). None of these rules is spec-mandated — all are library artifacts, mutually INCONSISTENT (apd `Pow(2,½)` ≠ float64 `Sqrt(2)`). | apd `Pow`: 34-sig-digit for negative/fractional exponents; exact for non-negative integer exponents; `Pow(0, neg) = Infinity`; `Pow(0,0)`/`Pow(-8,0.5)` error. float64 `Sqrt`: 17 digits + scientific notation; `Sqrt(-1) = NaN.0`. | Compute in EXACT DECIMAL throughout, ONE numeric model. (a) `Pow` non-negative integer exponent → exact repeated `DecimalValue` multiplication, integral results collapsed to int (byte-identical to cue). (b) `Sqrt` and `Pow(·, ½)` → `decimalSqrt` (fixed-iteration integer-Newton root scaled to ≥ 34 places, rendered to 34 sig digits via the shared division renderer); perfect squares collapse to int, exact-terminating non-integer roots TRIM to minimal scale (`Sqrt(2.25) = 1.5`, where cue's apd `Pow` pads `1.500…000`). One sqrt serves both, so `Sqrt(x) = Pow(x, ½)` (cue's are inconsistent). Domain errors bottom (no `NaN`/`Infinity`). Basis: exactness-first + self-consistency — the precision/format is spec-silent, so pick the exact-rational, internally-consistent rule. **(c) BI-2-§3 (2026-06-21) — full domain DONE, no longer deferred:** a negative-INTEGER exponent → `x^(-n)=1/x^n` (EXACT rational via `reciprocalDecimalToValue`); a general non-½ fractional exponent (`x>0`) → `x^y = exp(y·ln x)` via `decimalExpScaled`/`decimalLnScaled` — fixed-term Taylor (40-odd-term `artanh` for `ln`, 60-term for `exp`) + binary range reduction at working scale 50, rounded to 34 sig digits, integral results collapsed. PRECISION CHOICE (spec-silent): match cue's apd 34-sig context (mantissa byte-identical, verified on 40 random + extreme cases), but render trimmed/plain-decimal where cue pads/scientific (same display-divergence family as Sqrt). `Pow(0, neg)` → bottom (cue `Infinity`); `Pow(neg, non-int)`/`Pow(0,0)` → bottom (complex / cue error). NOTHING in the BI-2 family is now deferred. | exact `Pow` byte-identical; `Pow(·,½)` + general fractional 34-digit MANTISSA byte-identical to cue's apd `Pow`; `Sqrt`/perfect-square-collapse/`Sqrt(neg)` DIVERGE from cue's float64 `Sqrt` (Kue more precise + self-consistent — see `cue-divergences.md`); `Pow(0,0)`/`Pow(-2,½)`/`Pow(neg,non-int)` match-or-cleaner (Kue bottoms, cue errors/`Infinity`) | `builtins/math_pow`, `builtins/math_sqrt`; pins `BuiltinTests` `math_pow_*` / `math_sqrt_*` |
| `list.Sort` tie ordering + comparator value display (BI-2) | v0.16.1 | Silent on Sort STABILITY. cue's docs state `list.Sort` is "not guaranteed to be stable" but `list.SortStable` is; the spec does not mandate any particular order for elements the comparator deems equal (`less` false both ways). | `list.SortStable` is stable; `list.Sort` is "not guaranteed" stable but is observed stable on the comparators cue defines. Comparator VALUES `list.Ascending`/`Descending` display as `{T: number\|string, x: number\|string, y: number\|string, less: bool & x < y}`. | Use ONE total, stable merge sort (`sortValuesM`) for BOTH `list.Sort` and `list.SortStable` — a stable result is a valid `Sort` result (it satisfies the ordering), and stability is the strictly-stronger, more-deterministic, illegal-states-fewer choice (no nondeterministic tie order to reason about). Matches cue's observable order on every probed comparator. Comparator-VALUE display: Kue shows `less: number\|string < number\|string` (the `bool &` dropped — `x < y` already yields bool, and Kue's `meet(bool)(unresolved <)` eagerly bottoms, a pre-existing unrelated divergence) and renders the resolved `x`/`y` refs with their substituted slot types rather than cue's symbolic `x < y`. Display-only: a concrete pair binds `x`/`y`, so the sort comparison resolves to a real bool — the SORT RESULT is byte-identical. | yes for every sort result (Sort + SortStable, all probed comparators byte-identical); the standalone comparator-value `less` *display* differs (Kue `number\|string < number\|string` vs cue `bool & x < y`) — display-only, no sort drift | `builtins/list_sort`; pins `EvalTests` `eval_list_sort_*`, `eval_list_ascending_is_comparator_struct` |
| Import `:identifier` qualifier validity + default-name resolution scope (F-3) | v0.16.1 | Partial. The spec grammar pins the SHAPE — `ImportPath = '"' ImportLocation [ ":" identifier ] '"'`, and the PackageName "defaults to the identifier specified in the package clause of the imported package, which must match either the last path component of ImportLocation or the identifier following it" — but is silent on: (a) the exact validity boundary of the `:identifier` (is `_foo` allowed? is a `#`-definition id allowed?), and (b) whether the must-match check is a PARSE-time or LOAD-time obligation. | Parser accepts ANY suffix text (even non-identifiers — see `cue-divergences.md` F-3 row); enforces the must-match-declared-name rule at LOAD time (`no files in package directory with package name "other"` when the suffix mismatches the loaded `package` clause; `cannot determine package name … set it explicitly with ':'` when a bare path's last element is not a valid identifier). | (a) `isPackageIdentifier` admits a well-formed identifier (`foo`, `_foo`, `__`, `a1`) and rejects empty / digit-led / `#`/`_#` definition forms / the lone blank identifier `_` (cue specifically rejects `_` as a qualifier: `_ is not a valid import path qualifier`); `splitImportPath` additionally rejects an empty ImportLocation (`":foo"` / `""` — cue: `invalid import path`). This is the tightest reading of the spec's `identifier` production minus the spec's separate "PackageName must not be a definition identifier" rule and cue's blank-`_` carve-out. Validated at PARSE (illegal-states-unrepresentable — a malformed qualifier never reaches the loader; this is the F-3 divergence from cue). (b) Kue's F-3 scope is PARSE + bind-name + LOAD-time name gate: the qualifier is split into `Import.packageName` and made the local binding (precedence alias > qualifier > last-element; the binding is now purely LEXICAL — `importBindName` in `Value.lean`, no declared-name arm). The must-match-the-LOADED-declared-name check (cue's `package name "other"` mismatch error) is enforced since AUD-B6 (2026-07-06): `collectBindings` errors when the loaded `package` clause ≠ the expected name (qualifier, else last path element). A qualifier or bare path that mis-names its target package is now a cue-shaped load error, not a silent bind under the wrong name; well-formed correctly-matching imports resolve byte-identically to cue. | yes on the name-resolution axis — the qualifier SPLIT + bind-name defaulting + the load-time mismatch gate all match cue for every well-formed import (`qualified_import`, `_bare`, `_mixed`, `import_qualifier_pkgname_rescue` byte-identical; `import_bare_pkgname_mismatch` cue-shaped error); the identifier-VALIDITY gate remains stricter than cue (parse-rejects junk cue load-rejects — see F-3 `cue-divergences.md` row) | `modules/qualified_import{,_bare,_mixed}`, `import_{bare_pkgname_mismatch,qualifier_pkgname_rescue}`; pins `ParseTests` `parse_import_*`, `parse_is_package_identifier_cases`; `ModuleTests` `importBindName`/`expectedPackageName` examples |
| `strings.ToUpper`/`ToLower` case-mapping coverage + the deferred full-folding tail (BI-1) | v0.16.1 | The CUE spec does not define `strings.*` builtin semantics in prose; it defers to the stdlib, whose `ToUpper`/`ToLower` are Go's `unicode.ToUpper`/`ToLower` — the Unicode **simple** (1:1, non-length-changing) case mapping. The Unicode Standard is the real authority; `cue`/Go pick simple over full folding, and `cue` is a sound (non-buggy) data source for this. Silent on whether an impl must cover the whole BMP or may scope it. | Pure rune-wise **simple** mapping over the full BMP cased set (verified by querying the oracle over every BMP code point). NO length-changing special-casing: `ToUpper("ß")=="ß"` (not `"SS"`), `ToUpper("ﬁ")=="ﬁ"`. No locale tailoring (Turkish `ı`/`İ`), no context rules (final sigma). | Cover the full BMP via an oracle-derived **simple** 1:1 table (`Kue/CaseTable.lean`, generated by `scripts/gen-case-table.py` from the local `cue` oracle, READ-ONLY no network; lookup in `Builtin.lean`). CONFORMS to cue across the BMP — Kue intentionally matches the simple-mapping choice (so `ß`/`ﬁ` pass through, exactly as cue). Basis for the table over algorithmic ranges: the mapping is overwhelmingly irregular (632/674 ToUpper offset-runs are singletons), so a verified table is the only clean fully-correct path; matching `cue`'s simple mapping is a deliberate fidelity choice, not blind replication (the Unicode standard's full folding is the principled superset, deferred). **Deferred long tail (a future slice if a real case needs it):** full case folding (`ß`→`SS`, title-case digraphs), locale rules (Turkish dotless `ı`/`İ`), Greek final sigma, and astral-plane (non-BMP) cased letters (table is BMP-scoped). | yes — CONFORMS across the BMP (ASCII regression + Latin/Greek/Cyrillic round-trips + irregular singletons + `ß` no-expand all byte-identical to cue) | `builtins/strings_case_unicode`; pins `StringsTests` `strings_to_upper_*`/`strings_to_lower_*` (Unicode) + `case_table_lookup_*` |
| `strings.ToTitle` Unicode title-casing + word boundary (deferred; ASCII-bounded today) | v0.16.1 | Stdlib-deferred like ToUpper/ToLower: `cue`'s `ToTitle` upper-cases the first rune of each `unicode.IsSpace`-delimited word using the Unicode **title-case** mapping (distinct from upper — `ǆ`→`ǅ`, not `Ǆ`). The Unicode Standard is the authority. | Title-cases word-initial letters over the full Unicode cased set with `unicode.IsSpace` word boundaries: `ToTitle("über alles")=="Über Alles"`. | ASCII-bounded for THIS slice (BI-1 scoped to ToUpper/ToLower): Kue title-cases only ASCII word-initials and uses the six ASCII whitespace runes as the boundary; a non-ASCII word-initial is left unchanged (`"über alles"`→`"über Alles"`). NOT folded into BI-1 because it needs a separate Unicode **title-case** table (≠ the upper table) AND a `unicode.IsSpace` predicate. Recorded as a deferred capability (Kue does less than cue) — see `compat-assumptions.md` "String case folding" (a deferred capability, NOT a cue bug, so not in `cue-divergences.md`). | partial — ASCII title-casing matches cue; non-ASCII word-initial Kue (`"über Alles"`) ≠ cue (`"Über Alles"`) | `builtins/strings_case_unicode` (`titleNonAscii` records Kue's held form); pins `StringsTests` `strings_to_title_*` |
| `strings.ToCamel` Unicode lower-casing + word boundary (deferred; ASCII-bounded today) | v0.16.1 | Stdlib-deferred sibling of `ToTitle`: `cue`'s `ToCamel` maps the first rune of each `unicode.IsSpace`-delimited word to lower case via the Unicode **simple lower** mapping. Despite the name it does NOT camel-case — it only lower-cases word-initials. The Unicode Standard is the authority. | Lower-cases word-initial letters over the full Unicode cased set with `unicode.IsSpace` word boundaries: `ToCamel("Über Alles")=="über alles"`. | ASCII-bounded, sharing `mapWordInitial` with `ToTitle`: Kue lower-cases only ASCII word-initials (via `Char.toLower`) and uses the six ASCII whitespace runes as the boundary; a non-ASCII word-initial is left unchanged (`"Über Alles"`→`"Über alles"`). Same deferral as `ToTitle` — needs a `unicode.IsSpace` predicate and the full Unicode lower map on the word-initial. Recorded as a deferred capability (Kue does less than cue), NOT a cue bug, so not in `cue-divergences.md`. | partial — ASCII lower-casing matches cue; non-ASCII word-initial Kue (`"Über alles"`) ≠ cue (`"über alles"`) | pins `StringsTests` `strings_to_camel_*` |
| ✅ **RESOLVED → CONFORMING (E#4-fix, 2026-06-20)** — arithmetic operator domain (`+ - * /`) | v0.16.1 | **NOT silent — spec MANDATES the operand domain.** The spec: *"The four standard arithmetic operators (+, -, *, /) apply to integer and decimal floating-point types; + and * also apply to strings and bytes."* So a CONCRETE operand outside an op's domain (list/struct/bool/null for every op; string/bytes for `- /`) is **ill-typed → a type error (`_\|_`)**, the same class as `1 + "x"`. A spec mandate, not a gap. | cue HARD-ERRORS: `Addition of lists is superseded by list.Concat …` / `Multiplication of lists is superseded by list.Repeat …` / `cannot use [..] as type number` / `invalid operands … to '+'`. cue is **spec-correct**. Note: cue also implements `*` over (string\|bytes, int) as **repetition** (`"ab"*2 = "abab"`, `'ab'*2 = 'abab'`, either order; negative count errors), superseding strings/bytes.Repeat. | **FIXED (Kue now CONFORMS).** `classifyArithOperand` (`Eval.lean`) splits each operand into `prim` / `concreteNonArith` (a fully-evaluated `.struct`/`.list`/`.listTail`/`.embeddedList`) / `incomplete` (ref/kind/bound/unresolved-disj/…). `arithmeticDomainResult` then: an INCOMPLETE operand (either side) keeps the binary DEFERRED (it may still resolve to a number — cue holds `[1] + x` while `x: int`, erroring only after `x` resolves); a concrete-nonarith operand paired with a CONCRETE partner → type-error `.bottomWith [.nonArithmeticOperand op ty]`. The `prim,prim` mismatches (`1+"x"`, `"a"-"b"`, `"ab"*2.0`) were already `.bottom` and stay so. Separately, `evalMul` gained the string/bytes-`*`-int repetition arms (with `negativeRepeatCount` on a negative count). NOT a `cue-divergence` (cue was correct). | yes — CONFORMS across the matrix (every op × wrong-type combo err-vs-ok byte-matches cue; string/bytes repeat matches; incomplete defers like cue). The eval *display* of a deferred residual still renders the resolved ref by value (`int + [1]` vs cue's `x + [1]`) — the pre-existing D#1b-family display delta, value verdict identical. | `numeric/list_arithmetic_type_error`, `numeric/string_repeat_multiplication`, `numeric/arithmetic_incomplete_operand_defers`; pins `EvalTests` `eval_{add,sub,mul,div}_list_is_type_error`, `eval_add_struct_is_type_error`, `eval_mul_{string,int,bytes}_*_repeats`, `eval_add_list_incomplete_partner_defers` (+ symmetric/bound/ref defer pins) |
| **RECORDED (RESID-MASK-2, 2026-06-21)** — eager-prune-of-definitely-bottom vs hold of a disjunction whose surviving arm is still incomplete | v0.16.1 | Spec MANDATES *that* bottom alternatives are eliminated (Disjunction: *"distribute the unification over the alternatives and **eliminate bottom alternatives**"*; `("a"\|"b") & "c" == _\|_`) and treats `_\|_` as the `\|`-identity — but is SILENT on the *timing*: when one arm is DEFINITELY bottom (a terminal conflict) while another is still INCOMPLETE, may/must the bottom arm be eliminated EAGERLY (before the survivor concretizes), or may evaluation HOLD the whole disjunction? The spec also says *"Evaluation can retain unresolved disjunctions"*, which is the latitude cue uses. Genuinely silent on eager-vs-lazy elimination. | cue HOLDS the whole disjunction unresolved until a survivor concretizes — it does not evaluate a held residual arm far enough to see its terminal conflict, so it neither prunes the dead arm nor commits to the survivor. On `export` it surfaces `N errors in empty disjunction` (the dead arm's conflict + the survivor's incompleteness). Conservative/lazy, NOT spec-wrong. | **Kue ELIMINATES the definitely-bottom arm EAGERLY** and commits to the survivor even when the survivor is itself still incomplete — the more precise, total lattice move, directly grounded in the spec's "eliminate bottom alternatives" + `_\|_`-as-`\|`-identity. **SOUND (adversarially verified):** the prune gate (`liveAlternatives` → `containsBottom`) fires ONLY on a *materialized/terminal* `.bottom`/`.bottomWith` node — a conflict that has already reduced (`x:1 & x:2`, concrete-vs-bound `x:1 & x:>5`, disjoint-bound `x:>5 & x:<3`) and can never un-bottom under refinement. It NEVER fires on a merely-incomplete arm (bottom NOW only because an abstract operand has not resolved): an arm `a & {x:2}` with `a.x:int` is `{x:2,…}` (no bottom node), so it is NOT pruned and survives — pruning it would be the unsound case, and Kue does not. Where cue can still prune (concrete-narrowed shapes like `({x:>5}\|{x:<0}) & {x:7}` → `{x:7}`) the two AGREE. Basis: precise + total + "eliminate bottom alternatives" + illegal-states (a terminal bottom is unrepresentable as a live arm). | partial — Kue is MORE precise (eagerly resolves where cue holds → errors on export); they AGREE wherever the surviving arm is concrete (`_\|_`-identity, concrete-narrowed bound disjunctions). Not a divergence: cue's hold is permitted lazy eval, not a spec violation. | pins `TwoPassTests` `resid_mask2_witness_eager_prune_commits_to_incomplete_survivor`, `resid_mask2_sound_abstract_operand_arm_not_pruned`, `resid_mask2_sound_incomplete_arm_resolves_correctly_after_narrowing`, `resid_mask2_sound_both_incomplete_no_conflict_both_survive`, `resid_mask2_sound_bound_arm_survives_until_concrete_conflict`, `resid_mask2_precision_terminal_residual_arm_pruned_for_concrete_survivor`, `resid_mask2_bottom_identity_collapses_to_concrete_arm`, `resid_mask2_terminal_conflict_arm_sheds_for_concrete_survivor` |
| **RECORDED (TL-1, 2026-06-22)** — verdict for a call to an unknown / unimplemented builtin name | v0.16.1 | SILENT on the diagnostic, but the VALUE is decidable from first principles. The spec defines builtin calls only for the functions the stdlib exposes; it does not prescribe what an impl must do for a name that is NOT a known builtin (an unknown package, an unqualified non-builtin, or a stdlib function the impl has not implemented). A call to a non-function is not a value — by lattice first principles it is bottom (an unresolved reference), never a silent pass-through. The DIAGNOSTIC text (name-specific "reference not found" / "cannot call non-function" vs a generic bottom) is the genuinely-unpinned part. | cue rejects every such call (bottom, exit 1) with a NAME-SPECIFIC message: `reference "foobar" not found` (unknown package/unqualified), `cannot call non-function math.NoSuch (type _\|_)` (known package, unknown leaf), and a real-but-distinct value for `error("m")` (its message `m`). The verdict (bottom) is consistent; only the message is name-specific. | **Kue bottoms a non-builtin name with CONCRETE args** (a generic `(bottom)` reason), and DEFERS it with abstract args (a later pass may concretise). Mechanism: `BuiltinFamily.ofName?` classifies the name; `evalBuiltinCall` matches it exhaustively and routes the non-builtin (`none`) case through `unresolvedOrBottom` — the SAME concrete⇒bottom / pending⇒residual decision the in-family path already uses for an unknown LEAF. Basis: an unresolved builtin is bottom (lattice — not a value), total + illegal-states (the previously-silent residual fall-through is gone — a non-builtin name with concrete args can no longer manifest as a non-error `incomplete value`). Kue does not yet carry name-specific messages or `error`'s custom text (the message gap is the recorded divergence; the bottom VERDICT conforms). | yes on the VERDICT (both bottom an unknown name with concrete args, both defer-then-error an abstract one); the diagnostic MESSAGE is generic where cue's is name-specific (recorded in `cue-divergences.md`, TL-1 row — value agrees). | pins `BuiltinTests` `unknown_family_concrete_args_is_bottom`, `unknown_unqualified_name_concrete_args_is_bottom`, `unknown_error_builtin_concrete_arg_is_bottom`, `unknown_family_abstract_arg_stays_unresolved`, `unknown_family_bottom_arg_is_bottom`, `builtin_family_*` (classifier contract) |
| **RECORDED (scalar-embed-with-decls, 2026-06-22)** — a struct embedding a scalar PLUS non-output decls (`{#a:1, 5}`) | v0.16.1 | PARTIALLY SILENT. The spec says a struct whose only declarations reduce to an embedded value IS that value (the embedding/`v: {x}`→`x` rule), and that hidden/definition/optional/let members are non-output. It does NOT spell out the COMBINATION — an embedded scalar coexisting with surviving non-output decls: does the struct become the scalar while keeping the decls selectable, or does the scalar/struct mismatch conflict? The list form (`{#a:1,[1,2]}`) is the established precedent (Kue's `.embeddedList`); the scalar form is its direct analog and equally spec-grounded but not spelled out. | cue manifests `{#a:1,5}` as `5` and keeps `.#a` selectable (`x.#a → 1`); a regular/output field present (`{a:1,5}`) conflicts (mismatched struct/int); a no-decls `{5}` collapses to bare `5`; two distinct scalar embeds (`{#a:1,5,6}`) conflict; the carrier is NOT iterable (`for x in {#a:1,5}` → type error). | **Kue builds a dedicated `.embeddedScalar scalar decls` carrier** — the scalar analog of `.embeddedList`. The scalar manifests; the decls ride alongside and stay selectable; a genuine scalar conflict surfaces as an INLINE bottom in the carrier (the RESID-MASK convention `.embeddedList` uses), so `containsBottom` flags it and export rejects (matching cue's whole-value reject). Basis: lattice first principles + the established `.embeddedList` precedent — a struct reducible to a scalar IS that scalar, losslessly carrying its non-output declarations. The SOUNDNESS BOUNDARY held: the pure `{5}` collapse (no decls) was left untouched — widening it to admit decls is the unsound direction (it would DROP them); the carrier is a SEPARATE path. | YES — Kue's manifest/selection/conflict/non-iterable/operand-position behavior matches cue v0.16.1, AND the one over-merge the slice's "every case tried" claim missed is now FIXED (CARRIER-STRUCT-MEET, 2026-06-22): a carrier met with a PURE decls-only struct that has NO embed of its own (`{#a:1,5} & {#b:2}`) now BOTTOMS — the carrier IS its scalar, so `5 & {#b:2}` is an int-vs-struct kind conflict (`_\|_`), spec + cue agree. Was previously MORE PERMISSIVE (wrongly merged the decls); the fix routed the decls-only-struct sub-case in all four carrier meet arms (`Lattice.lean`, both carriers × both operand orders) to `meetCore` → bottom. The identical fix applied to `.embeddedList`. carrier & carrier (both reduce to scalars/lists) MERGES and carrier & output-field-struct BOTTOMS — both unchanged, both correct. The COMBINATION stays recorded here (still spec-silent — matching a binary artifact is lower-confidence than a mandate — and the carrier is a principled choice mirroring `.embeddedList`), but Kue now CONFORMS on every sub-shape. | pins `EvalTests` `scalar_embed_with_decls_*`, `soundness_*`, `scalar_carrier_*` (operand-position + meet edges), `meet_{scalar,list}_carrier_with_declsonly_struct_bottoms` + symmetric/multi-decl analogs, `{scalar,list}_carrier_meet_carrier_keeps_all_decls`, `{scalar,list}_carrier_meet_output_field_struct_bottoms`; `ListTests` `meet_{scalar_carrier,embedded_list}_with_decls_struct_bottoms` (+ symmetric) / `meet_two_{scalar_carriers,embedded_lists}` / `manifest_scalar_carrier_is_scalar`; fixtures `structs/scalar_embedding_{with_decls,decl_select,multiple_decls,operand_position}`, `comprehensions/for_over_embedded_list` |
| ✅ **CONFORMING (Bug2-10, 2026-06-23)** — closedness when a struct EMBEDS a closed def: embed-form sibling vs meet-form extra | v0.16.1 | The spec says embedding "is unified with the struct in which it is embedded" and that closedness is monotone through meet, but does not spell out the asymmetry between a sibling field declared in the SAME struct literal as the embed vs a field added by a LATER `&`. | cue is SELF-CONSISTENT here (unlike the pattern-only EMBED-CLOSE-1 case): `{#Meta, b}` (sibling `b` in the embed's literal) ADMITS `b`; `{#Meta} & {b}` (a separate meet against the now-closed struct) REJECTS `b` (`field not allowed`). Both spec-faithful — embedding a closed def closes the result over `host ∪ embed` labels, and `b`-in-the-literal is part of the host's own declaration BEFORE that close. | **Kue now CONFORMS on BOTH spellings.** A PRE-EXISTING leak (kue admitted the meet-form `{#Meta} & {b}` — reproducible with NO self-ref, so not introduced by Bug2-10's delivery fix) was closed via `embeddingClosesHost`/`embeddingFieldIsDefinition`: a definition-class embed (closed even when its UNEVALUATED body is still `regularOpen`) overrides the host's `regularOpen` in `closeEmbeddedOver`'s openness arg — ONLY for `regularOpen` (an explicit `...`/`defOpenViaTail` host stays open). The embed-FORM sibling stays admitted (it is in the merged static frame before the close, and is in the allowed-label union). Basis: lattice closedness-monotonicity + the spec's embedding-is-unification rule. | yes — both `{#Meta, b}` (admit) and `{#Meta} & {b}` (reject) byte-match cue v0.16.1 | pins `TwoPassTests` `bug210_embed_meet_extra_rejected` (meet-form rejects), `bug210_embed_form_sibling_admitted` (embed-form admits), `bug210_embed_closed_rejects_extra` (use-site narrowing case rejects); fixtures `definitions/bug210_embed_{meet_extra_rejected,form_sibling_admitted,closed_rejects_extra}` |
| **RECORDED (Bug2-14b/c abstract-kind disjunction-arm, 2026-06-23)** — a `#Mixin`-shaped structural disjunction (`listShape \| structShape \| error`) whose `let _patch` guard (`if kind == add.#kind`) reads a sibling `kind` left ABSTRACT (`string`) | v0.16.1 | SILENT on the verdict for an un-narrowed structural disjunction whose surviving arm's comprehension guard is genuinely UNDECIDABLE. The disjunction/default rules govern narrowing + bottom-arm elimination; the comprehension rules say an incomplete `if` guard holds the residual. The COMBINATION — a struct-shaped arm survives shape-dispatch but its buried let-local guard cannot decide — is not spelled out: hold the comprehension residual (incomplete) or fall through to the `error` arm? | cue picks the `error(…)` arm — it reports `#Mixin: target must have #components or kind: string` (the un-narrowed struct fails shape-dispatch into a usable `structShape`, so the only surviving arm is `error`). | **Kue HOLDS the comprehension residual: it DEFERS as `incomplete value` (the un-drained `for … if kind == add.#kind {…}` node), it does NOT fall through to the `error` arm and does NOT force-drain a wrong value.** Both are correct NON-drains (neither emits spurious annotations); they differ only in WHICH rejection (cue: `error`-arm message; kue: `incomplete`). Basis: precise + total — when a guard is undecidable the spec's comprehension rule holds the residual; collapsing to the `error` arm would over-commit (a later meet narrowing `kind` could still fire the guard and select `structShape`). The eager-arm-elimination convention (RESID-MASK-2) prunes only TERMINAL bottoms; `structShape` here is INCOMPLETE-not-bottom, so it is not pruned, and the disjunction holds. | partial — both REJECT (no spurious drain); cue via the `error` arm, kue via `incomplete value`. Not a divergence in VALUE (neither drains), only in the rejection diagnostic. | pins `Bug2xTests` `bug214b_disj_arm_incomplete_guard_defers` (export bottoms — the residual is held, not drained); module fixture `bug214b_disjarm_letlocal_force` (the decidable counterpart drains == cue) |
| ✅ **RESOLVED → cue-divergence (Bug2-12 MUTUAL, 2026-06-23)** — closedness through a MUTUAL-recursion def cycle (`#A: #B & {a:1}`, `#B: #A & {b:2}`) | v0.16.1 | PARTIALLY SILENT. The closed-meet rule (`#A & #B` concatenates allowed-sets) and the structural-cycle rule (a self/mutual struct cycle bottoms) are each spec-pinned, but their COMBINATION was not: does `#A` reject `a` (use-site add to closed `#B`) or admit `a` (its own declared field)? **ADJUDICATED:** a def's closed allowed-set is the TRANSITIVE union of all cycle members' declared labels (`#A = #B & {a} = #A & {a,b}` ⟹ `allowed(#A) = {a,b}`); closedness BOUNDS use-site additions, it NEVER rejects a label the def itself declares — so the principled answer ADMITS `{a,b}`, REJECTS a genuine extra `c`. | cue REJECTS even the def's own field (`#A.a: field not allowed`) — closing `#B` PREMATURELY mid-cycle. This is a cue BUG (over-reject), not the spec; internally inconsistent with cue's correct acyclic behavior (a def's declared field is never rejected). **Recorded as a cue-divergence** (`cue-divergences.md`: "Mutual-recursion closed def rejects its OWN declared field"), not matched. | **Kue now CONFORMS to the principled answer (FIXED):** `out: #A` → `{a:1,b:2}` (admit transitive declared); `out: #A & {c:3}` → bottoms `c` (reject genuine extra). The pre-fix UNDER-CLOSE (admitted `c`) is fixed via `defSlotInClosedCycle`: the `flattenConjDefRef` `close` gate now fires for any depth-0 def→def cycle reaching this slot (not only a DIRECT self-ref), and the existing Bug2-12b union-then-close-once machinery fixes the allowed-set to the transitive `{a,b}`. The transitive flatten already pulls every cycle member's literals into `expanded`. The one-way distinct-meet (`#A: #B & {a}`, `#B: {b}`) stays on its existing path (NOT a cycle, `defSlotInClosedCycle` returns false). | yes (Kue conforms to the lattice-principled answer; cue over-rejects — see `cue-divergences.md`). Self-rec + one-way + distinct-meet + mutual all conform. | pins `Bug2xTests` `bug212_mutual_*`; fixtures `definitions/bug212_mutual_*` |
| **RECORDED (parser-strictness `__`-reservation, 2026-06-23)** — does the `__`-prefix keyword reservation extend to PACKAGE NAMES and IMPORT-PATH QUALIFIERS? | v0.16.1 | AMBIGUOUS for these positions. The spec text "CUE reserves all identifiers starting with `__` as keywords" clearly governs ordinary identifiers (field labels, references, aliases — Kue now REJECTS those, spec-conforming). It is silent on whether a PackageName / import-qualifier identifier — a distinct grammar production — is also reserved. | cue is INTERNALLY INCONSISTENT here: `import __bar "..."` (an alias) is rejected `identifiers starting with '__' are reserved`, but `package __pkg` is ACCEPTED, and an import-qualifier `"loc:__foo"` errors at module-resolution before any reservation check fires (inconclusive). | **Kue REJECTS `__`-prefixed aliases** (they flow through `parseIdentifier`, matching cue's alias rejection) but **leaves the dedicated `isPackageIdentifier` validator UNCHANGED** — it still accepts a `__`-prefixed package qualifier. DELIBERATELY out-of-scope for the parser-strictness slice: the two NAMED items (field/ref `__x`, `*(1|2)`) are spec-clear; the package-name corner is murky in BOTH spec AND cue, and tightening it risks over-rejecting a position the spec does not clearly reserve. Deferred on spec-murkiness (not a usage judgment); pick up as a dedicated loader-strictness slice when the spec position for these grammar productions is clarified. | partial — aliases match cue (reject); package-name qualifier diverges (Kue accepts `__`, cue accepts `package __pkg` too but rejects import positions inconsistently) | not pinned (out-of-scope); the in-scope `__` rejections pinned in `ParseTests` `parse_double_underscore_*` |
| **RECORDED (A2-y import-name redeclaration, 2026-06-23)** — the precise EXEMPTION boundary of the import-name redeclaration check (which field forms collide with an import's bound local name) | v0.16.1 | MOSTLY pinned, with one silent corner. The spec mandates the core ("No identifier may be declared twice in the same block": an import binds the package name in the file block, a same-name bare-identifier top-level field re-declares it — so the collision IS a load error). It does NOT spell out the exemptions; those follow from first principles: a quoted-string label is a string label not an identifier declaration, and `#x`/`_x` live in distinct (definition/hidden) namespaces — none declares the bare identifier in the block. The ONE genuinely-silent corner: an ALIASED field whose own label collides (`x=dep: 1` with `import "…/dep"`). | cue REJECTS the bare/optional/required forms and the alias/qualifier bound name; ACCEPTS quoted `"dep"`, `#dep`, `_dep`, nested, and a different-named field (all confirmed by oracle). For the aliased-field corner `x=dep: 1`, cue SHORT-CIRCUITS on a DIFFERENT error first (`unreferenced alias or let clause x`), so its redeclaration verdict there is unobservable. | Kue matches cue on every confirmed case via `bareIdentifierLabels` (eligible = bare `quoted=false`, class `.field false false _` — all three presence rungs; quoted/`#`/`_`/`let`/embedding/pattern excluded). The aliased-field corner is DELIBERATELY EXEMPTED (the `.fieldAlias` parse node is not collected into `bareIdentifierLabels`) — the conservative, no-over-reject choice: cue's verdict there is masked by its own alias error, and exempting cannot reject valid code (the conservative, no-over-reject choice). The corner is spec-silent AND cue-unobservable; revisit if the spec or an observable cue behavior clarifies it. | partial — every observable cue case matches (collide: bare/`?`/`!`/alias-name/qualifier-name/builtin-name; exempt: quoted/`#`/`_`/nested/different-name); the aliased-field-label corner is exempted-by-choice (cue's verdict unobservable there). | pins `ModuleTests` (`bareIdentifierLabels` eligibility + exemptions, `checkImportRedeclaration`); fixtures `modules/import_name_redeclaration` + `modules/import_name_no_collision` |
| **B3d-5 (2026-06-26)** — `cue.sum` integrity-sum file | v0.16.1 | Silent on the LANGUAGE side, but the TOOLING reality is: cue v0.16.1 ships **no** `cue.sum` mechanism at all — there is no `HashZip`/`golang.org/x/mod/sumdb/dirhash` consumer in its source, and nothing reads or writes a `cue.sum` file. The OCI blob `sha256:` digest (carried in the image manifest) is the only live integrity check on a fetched module. | Verifies the fetched blob against the manifest `sha256:` digest (in `fetchBlob`/`curlGetVerified`); no `cue.sum` file is consulted, produced, or required. | **Verify the OCI blob digest as the primary integrity gate (matches cue); ADDITIONALLY honor a `cue.sum` `h1:` line when one is present** — defensive + forward-compatible (a future cue, or a hand-authored sum, is enforced; an absent file is no error, matching cue v0.16.1). The `h1:` algorithm is the Go `dirhash.Hash1` port already built in B3d-3 (`Sha256.hash1`). Basis: illegal-states + defense-in-depth — a recorded sum that mismatches the bytes MUST reject the install, never silently proceed; but absence of cue-side sums must not block real modules. WRITING/maintaining `cue.sum` (`cue mod tidy`) is deferred to B3d-6. | yes (no `cue.sum` ⇒ identical to cue; the extra `h1:` check only fires on a sum cue never writes) | `scripts/check-fetch-pipeline.lean` (cue.sum accept + reject paths); `Sha256Tests` `hash1_*` |
| **let/alias no-shadow — REVERSE direction (2026-07-04, CLOSED — both directions land)** | v0.16.1 | Spec: a `let`/value-alias may not collide with a field of the same name in a comparable lexical scope (the `alias` scoping rule). cue enforces this symmetrically — field ancestor OR descendant of the `let` both reject (`cannot have both alias and field with name "x" in same scope`). | Rejects a `let x` shadowed by a field `x` whether the field is in an enclosing OR a nested scope (both directions). | **RESOLVED.** The blocker (a descendant field's quoted-accurate name is unavailable post-parse) was fixed by modeling quoting on the `Value` layer: `Field.quoted` (a parse-time provenance bit, set only for a genuinely-quoted `"x":` label at `parseQuotedLabelField`). Since ARCH-QUOTED-STRIP (2026-07-05) `quoted` is the `Quoted` newtype — inert to `Value`/`Field` equality by construction (its `BEq` ignores the payload) yet still read by `collidableFieldLabel` — so the reverse check stays quoted-accurate with no `stripFieldQuoting` normalization pass. `checkLetFieldShadow` now runs BOTH directions at every struct scope via ONE predicate-parameterised traversal (`collectMemberLabels`): FORWARD = top-level field ∩ subtree `let`s; REVERSE = top-level `let` ∩ subtree fields (`collectFieldNames`, quoted-accurate — a `"x"` label is excluded, so an ancestor `let x` never falsely collides with a descendant `"x":`). Every descendant scope is comparable to the anchoring struct and incomparable cousins anchor at distinct structs, so neither direction fires across incomparable scopes. | yes (both directions; quoted/def/dynamic/for-var/comprehension-let/incomparable-sibling all still accept) | GRADUATED: `testdata/wild/let-shadowed-by-nested-field`, `…-by-descendant-field-in-struct`, `…-by-field-in-def-body` (`.known-red` removed); 8 new reverse probe theorems in `ParseTests.lean` |
| **Typed struct ellipsis `{...T}` (TYPED-ELLIPSIS-PROBE, 2026-07-04)** | v0.16.1 | The CUE spec grammar RESERVES `Ellipsis = "..." [ Expression ]` but marks the typed form (`...T` with `T ≠` top) not-yet-implemented. So the LANGUAGE status is "reserved, unimplemented" — not "silent"; the spec explicitly declines to give `{...T}` a meaning yet. The Value-level SEMANTICS of a typed tail coexisting with `[pattern]` constraints is genuinely under-specified, but is UNREACHABLE from source (see behavior). | cue v0.16.1 REJECTS `{...T}` at parse for every `T` including `..._` ("missing ',' in struct literal"); only bare `{...}` parses. It never constructs a typed struct tail. (Lists differ: `[...T]` IS parsed and enforced as an element constraint.) | **REJECT `{...T}` at parse, matching cue** (`Parse.lean:1483` "typed struct ellipsis is not supported yet"; only bare `...` yields `.ellipsis .top`) — so a struct `tail` only ever carries `.top` from source. Basis: conform to cue AND to the spec's reserved-but-unimplemented status; implementing typed struct ellipsis would DIVERGE from both. The internal `Value`/`meet` machinery ALREADY applies a typed tail SOUNDLY should one ever be constructed (rejects wrong-typed additional fields, exempts own declared fields — `StructTests` `meet_typed_ellipsis_*`), and `applyEvaluatedStructN` PRESERVES the tail — but this is dormant, gated behind the parse rejection. `[...T]` lists are parsed and match cue exactly (`[...int] & [1,"s"]` → bottom). | yes (both reject `{...T}` at parse; both parse+enforce `[...T]`; kue byte-matches cue on lists) | `ParseTests` `parse_struct_typed_{ellipsis,top_ellipsis}_rejected`, `parse_struct_ellipsis_{schema,unification}` (bare `...`); `StructTests` `format_typed_ellipsis` + `meet_typed_ellipsis_*` (dormant Value-level semantics); `parse_list_typed_tail` round-trip |
| Eval-display of a deferred (incomplete-bound) list slice (LIST-SLICE) | v0.16.1 | Silent. `eval` is a presentation mode; the spec pins slice VALUE semantics (half-open, bounds errors, incomplete-index defers) but not how a HELD/residual slice is rendered. | Eval-holds the source form `l[x:2]` and surfaces the incompleteness only at `export` (`incomplete int in slice index: int`, exit 1). | Kue desugars `x[lo:hi]` to the core `slice(base, lo, hi)` builtin (import-exempt, distinct from the import-gated public `list.Slice` since 2026-07-05 BUILTIN-IMPORT-LENIENCY), so a deferred slice (incomplete bound) renders in `eval` as `slice([1, 2, 3, 4], int, 2)`, NOT `l[x:2]`. The VALUE verdict agrees — it is a residual `.builtinCall` that bottoms as incomplete at `export`, exactly as cue does. Same display-only family as the deferred-comprehension row in `cue-divergences.md`: same value + same export error class, different residual surface. Basis: reusing the tested builtin-defer pipeline (illegal-states-fewer than a bespoke slice ctor threaded through 11 files) is worth a cosmetic residual-display artifact that never touches the observable `export`. | partial — VALUE/`export` verdict matches cue (incomplete → error); eval residual display differs (`slice(…)` vs cue's held `l[x:2]`) | `SliceTests` `slice_incomplete_index_defers` (pins the residual); export matrix in `testdata/export/list_slice` (concrete cases) |
| **BUILTIN-IMPORT-LENIENCY (2026-07-05)** — resolving a qualified stdlib builtin (`strings.ToUpper`, `list.Concat`, …) WITHOUT the `import` statement cue requires | v0.16.1 | Silent on this NON-CORE feature surface. The spec defines the stdlib packages and the `import` declaration, but does not pin whether a qualified builtin reference is usable with NO preceding `import "<pkg>"` — the import obligation for builtin packages is a loader/tooling rule the language spec does not spell out. Genuinely spec-silent. | REQUIRES the import: `strings.ToUpper("x")` with no `import "strings"` errors (`reference "strings" not found`); the qualified reference resolves only after the package is imported. | **ENFORCE cue's `import` requirement (cue-compat) — ✅ LANDED 2026-07-05.** A qualified builtin reference (call `strings.ToUpper(...)` OR constant `list.Ascending`) resolves ONLY when its package is imported; an un-imported one is `.bottomWith [.unresolvedReference "<pkg>"]` → `reference "<pkg>" not found` (bottom), matching cue. Enforced in the import-aware post-parse pass (`applyBuiltinAliases` → `canonicalizeBuiltinCalls`, with `gateBuiltinImport` for call heads and `resolveBuiltinConstSelector` for the deferred constant form; `importedBuiltinPackages` supplies the accepted-package set) — the single choke point both `parseDocument` (single-file/inline) and `parseDocumentFile` (module-load) pass through, so per-file import scope is honored exactly as cue's file-scoped imports. Aliased imports (`import s "strings"`) resolve via the existing alias-canonicalization. The slice OPERATOR `x[lo:hi]` desugars to a new import-exempt core `slice` builtin, distinct from the import-gated public `list.Slice`. Corpus migration was ZERO fixtures (all builtin-using fixtures already imported, being cue-oracle-derived). Per spec-silent → cue-compat tiebreak + illegal-states-unrepresentable. | yes (kue now matches cue: un-imported qualified builtin → `reference not found`; imported → resolves) | LANDED: `ImportEnforcementTests.lean` (17 theorems: call/const forms, aliased/wrong/multiple/nested imports, slice-operator exemption vs public `list.Slice`, local-field shadowing); `ParseTests` `resolve_builtin_const_selector_gates_on_import` |
| **FLOAT OUTPUT FORM (GDA-FLOAT-RENDER, 2026-07-05)** — the textual form a `Prim.float` renders to per output surface | v0.16.1 | Silent on float DISPLAY. The spec pins float VALUE semantics (a decimal literal is an exact base-10 value) but not the canonical text a formatter emits — plain vs scientific threshold, `E` casing, trailing-zero/whole-number form. `cue` inherits apd's General-Decimal-Arithmetic `to-scientific-string`, an implementation artifact. | `cue` renders from the literal's apd `(coefficient, exponent)` form (representation-dependent: `1e2`→`1E+2` but the value-equal `1.00e2`→`100`), with per-surface casing/tails: JSON uppercase `E` + bare whole floats (`100`, `1E+40`); YAML uppercase `E` + trailing-dot whole floats (`100.`); cue-native lowercase `e` + `.0` whole floats (`100.0`, `1e+40`). Small exponents expand (`1e-2`→`0.01`), the plain/scientific switch is adjusted-exp `≥ −6` (`1e-6`→`0.000001`, `1e-7`→`1E-7`); literal `-0.0`→`0.0`. | Kue matches `cue` on every surface: `renderFloatText` derives the apd form from the retained source `text` (the normalized `DecimalValue` cannot — a non-negative `scale` multiplies a positive exponent into the coefficient, collapsing `1e2`/`1.00e2` and erasing the `1e40` scientific switch), then applies `to-scientific-string` with a per-surface `FloatRenderStyle` (`jsonFloatStyle`/`yamlFloatStyle`/`cueFloatStyle`). Representation-dependence is deliberate continuity with FLOAT-UNIFY-EQUAL (float rendering already keeps the operand's text: `1.0 & 1.00 = 1.0`), not a new choice. Basis: the spec does not mandate a canonical float form, so a spec-silent surface defaults to `cue`-compat; the apd-from-`text` mechanism is the only faithful reconstruction. | yes (byte-identical across JSON/YAML/cue-native on the full matrix incl. edges) | `testdata/export/float_render_gda.{cue,json,yaml}`, `testdata/cue/numeric/float_gda_render.expected`; `FloatTests` `float_apd_form_faithful`/`float_render_json`/`float_render_cue_native`/`float_render_yaml` |
| **BYTE-LITERAL FORMATTING (BYTE-ARRAY-REPR, 2026-07-05)** — how a `Prim.bytes` value round-formats to CUE syntax | v0.16.1 | Silent on byte-literal DISPLAY. The spec pins byte-literal VALUE semantics (a `'...'` literal is a sequence of raw bytes, `\xNN`/`\NNN` naming one octet) but not how a formatter re-renders a byte value — single- vs triple-quote, which bytes escape, named vs hex escapes. | `cue eval` promotes a byte value containing a newline to a TRIPLE-quote literal (`'''\n\tabc\n\tdef\n\t'''`), rendering the raw bytes across lines; it never emits a single-quote literal with `\xNN` for control/high bytes in that case. | Kue renders bytes as a SINGLE-quote literal with INLINE escapes: `\n\r\t` named escapes, `\'`/`\\` for quote/backslash, printable ASCII (0x20–0x7E) verbatim, and `\xNN` hex for every other control or ≥ 0x80 byte (`formatByte`/`formatBytesLiteral` in `Format.lean`). So `'''abc\ndef'''` re-formats to `'abc\ndef'` (not cue's triple-quote block), and `\xff` re-formats to `'\xff'`. Basis: exact, unambiguous, round-trippable through `parseQuotedBytes` — a single-quote literal with escapes is illegal-states-fewer than cue's triple-quote promotion (no line-continuation/indent-strip ambiguity), and cue's triple-quote multiline promotion is a display feature Kue does not implement. cue is NOT wrong here (its output is spec-valid too) — this is a formatter style choice on a spec-silent surface, not a divergence. | display-only — the exported VALUE (`export` base64) is byte-identical to cue (`'''abc\ndef'''` and `'abc\ndef'` both export `YWJjCmRlZg==`); the CUE-syntax `eval`/format rendering differs (Kue single-quote inline-escape vs cue triple-quote block) | `testdata/cue/multiline/multiline_bytes.expected` (Kue single-quote form); `BytesTests` `format_bytes_high_byte`, `format_bytes_kind_and_primitive` |
| **MODULE.CUE REFORMATTING (`mod get`, B3d-6b-leg2, 2026-07-05)** — whether `cue mod get` may rewrite non-deps content of `cue.mod/module.cue` | v0.16.1 | Silent. The CUE spec governs the module.cue SCHEMA (`module`, `language`, `deps`, `source`), not the on-disk formatting a mutation tool applies. Whether editing one field may reformat the rest of the file, drop comments, or expand shorthand is a tool presentation choice, not a value/schema mandate. | Runs its full CUE formatter over the WHOLE file: expands one-line shorthand to block form (`language: version: "…"` → `language: {\n\tversion: "…"\n}`), drops comments, canonically re-orders fields — even the parts the edit did not touch. | **PRESERVE non-deps content verbatim; re-render ONLY the `deps` block.** kue excises the existing top-level `deps` block (string/brace-aware textual scan) and splices a canonically-rendered one back, leaving every other byte — comments, shorthand, unknown/`source:` fields — exactly as written. Basis: minimal-diff + illegal-states-unrepresentable — a mutation tool that reformats untouched content risks data loss (an unknown field a bespoke printer doesn't model would vanish) and noisy diffs; a byte-identical whole-file match would require a full CUE pretty-printer kue does not have and the spec does not require. The `deps` block ITSELF is byte-identical to cue (canonical tab-indented, keys sorted). If a present deps block cannot be located textually (an exotic hand-written form), kue ERRORS rather than emit a conflicting second block. | partial — the emitted `deps` block is byte-identical to `cue mod get` v0.16.1 (verified on the canonical block-form add); non-deps content diverges (kue preserves, cue reformats) — spec-silent, no value/schema impact | pins `ModCmdTests` `renderDepsBlock`/`exciseTopLevelDeps`/`modGetResolveAndApply` examples; manual `diff` vs `cue mod edit --require` (block-form add byte-identical) |
| **UNUSED-IMPORT (2026-07-05)** — declaring an `import` a file never references (the mirror of BUILTIN-IMPORT-LENIENCY's used⇒declared) | v0.16.1 | Silent on the DIAGNOSTIC. The spec defines the `import` declaration but does not spell out the obligation that every declared import be used, nor the message an impl must emit — the `declared ⇒ used` rule is a loader/tooling contract the language spec leaves to the impl. The VERDICT (an unused binding is dead, a file that carries one is ill-formed) follows from the same dead-binding principle as the unreferenced-`let` case; the MESSAGE text is the unpinned part. | REJECTS the file at build: `imported and not used: "strings"` (plain), `imported and not used: "strings" as s` (aliased), `imported and not used: "encoding/json"` (path form) — exit 1, per unused import. | **ENFORCE cue's `declared ⇒ used` requirement (cue-compat) — ✅ LANDED 2026-07-05.** An `import` whose local bind name is never referenced in the file body collapses the document to `.bottomWith [.importedNotUsed path alias]` (one reason per unused import, carrying the PATH + optional alias so the cue `"<path>" as <alias>` form is reconstructable). Detection is a pre-canonicalization walk (`collectReferencedHeads`, `Kue/Parse.lean`) gathering every referenced package head — selector bases, aliased builtin-call heads, deferred stdlib-const heads, bare refs — checked in `resolveImports` against each import's `importLocalBindName`; the walk only under-reports so a used import is never mis-flagged. Runs at both parse entry points (`parseDocument`, `parseDocumentFile`), honoring per-file import scope. Where a PRIOR error applies (import-name redeclaration, invalid package id) the loader's own check supersedes, matching cue's precedence. Per spec-silent → cue-compat tiebreak + illegal-states-unrepresentable. | VERDICT and MESSAGE both match cue (an unused import → the file bottoms, exit 1, rendered `imported and not used: "<path>"`). The MESSAGE residual is CLOSED by STDLIB-E (2026-07-10): `Manifest.manifestWithFuel` routes a `.bottomWith` carrying `.importedNotUsed` reasons (via `unusedImportReasons`) to `ManifestError.importedNotUsed`, which `Runtime.formatManifestError` renders as cue's per-import line (`" as <alias>"` when aliased, one line each for multiple; no source position — the reason carries none). | LANDED: `ImportEnforcementTests.lean` (unused single/aliased/grouped/multiple, reason-pinned path+alias, use-site coverage: nested struct, interpolation, comprehension, definition, const form; STDLIB-E render pins `*_render_message`); `ParseTests` `parse_unused_import_clause_bottoms`/`parse_unused_grouped_import_clause_bottoms`/`parse_used_import_clause_parses_body`; wild `testdata/wild/{unused-import,unused-import-aliased,used-import-ok}/` |
| **STDLIB-IMPORT ROUTING (2026-07-10, stdlib campaign slice A)** — how a recognized-but-unimplemented standard-library import (`strconv`, `struct`, `time`) is routed and diagnosed | v0.16.1 | Silent on the exact routing mechanism + error text, but the STRUCTURAL distinction is spec-grounded: CUE separates stdlib/builtin imports from external-module imports by whether the first path element carries a domain — a stdlib path's first element has NO dot (`strconv`, `encoding/hex`, `text/template`), an external module path's first element is a domain (`example.com/foo`). The routing rule (dot-free ⇒ builtin layer) follows from this; the impl's error MESSAGE for a recognized-but-unimplemented builtin is the unpinned part. | Recognizes `strconv` etc. as stdlib and resolves the import PATH via the builtin layer (errors only later on the unimplemented FUNC, if any). kue previously misrouted every non-whitelisted path — dot-free stdlib paths included — to the disk module loader, surfacing the misleading `no cue.mod/module.cue found` error. | **ROUTE structurally, not by whitelist membership — ✅ LANDED 2026-07-10.** `isStdlibImportPath` (`Kue/Value.lean`) classifies an import by its first path element: dot-free ⇒ stdlib/builtin, dotted ⇒ external module. `isUnimplementedBuiltin` = stdlib path absent from `builtinImportPaths`. The loader (`collectBindings`, `loadFileBound`, `Kue/Module.lean`) now emits a clear `unsupported builtin package "<path>": recognized as a CUE standard-library import but not yet implemented in kue` for such paths — never the disk-loader error. External (dotted-domain) paths keep routing to `resolveImportTarget` unchanged. Message text is kue's principled choice (spec-silent); scope is ROUTING + error quality only — the struct/strconv function bodies are later slices B/C. Per spec-silent → cue-compat tiebreak + illegal-states-unrepresentable. | ROUTING matches cue (stdlib paths handled by the builtin layer, not disk); the ERROR is kue-specific (cue implements these packages, so it never emits an "unimplemented builtin" message — a divergence only because kue's builtin coverage is a subset, not a semantic disagreement). | `testdata/wild/stdlib-import-misrouted-to-disk-loader/` (auto-discovered by `check-fixtures.sh`; pins the `unsupported builtin package "strconv"` error substring) |
| **STDLIB-STRUCT-FIELDCOUNT (2026-07-10, stdlib campaign slice B)** — the exact counting rule for `struct.MinFields(n)` / `struct.MaxFields(n)` | v0.16.1 | Silent. `struct.MinFields`/`MaxFields` are STANDARD-LIBRARY package builtins, not language constructs — the CUE language spec pins unification/closedness/field presence, but not which fields a stdlib validator tallies. The VALIDATOR shape (unifies with a struct, satisfied iff the count meets the bound) follows from lattice-theoretic first principles (a constraint that participates in `meet`); the precise inclusion set (does a required/optional/hidden field count?) is a stdlib-package behavior the spec does not adjudicate. | Counts only REGULAR fields. Empirically pinned vs cue v0.16.1: `{a:1,b:2} & struct.MinFields(2)` ok; optional (`{a:1,b?:2} & MinFields(2)`), **required (`{a:1,b!:2} & MinFields(2)`)**, and hidden (`{a:1,_b:2} & MinFields(2)`) all fail at `1 < 2` — so required/optional/hidden/definition/`let` are ALL excluded. `MinFields(0)` on `{}` ok; `MinFields(-1)` trivially ok; `MinFields(1.5)` a type error; a bare `struct.MinFields(0)` is `incomplete`. | **cue-compat: count only REGULAR fields (`FieldClass.countsAsField` = `.field false false .regular`).** `Value.lengthConstraint .fields (bound : CountBound) (limit : Int)` (`Kue/Value.lean`) is a validator participating in `meet` (`applyLengthConstraint`, `Kue/Lattice.lean`); satisfied `min` drops, violated `max` bottoms, the undecided residual retained in a `.conj` and adjudicated at manifest (`finalizeLengthConj`, extended 2026-07-10 to finalize retained residuals nested in a disjunction arm too — see FIELDCOUNT-DISJ below) so cross-conjunct accretion is spec-correct. Basis: spec-silent non-core surface → cue-compat tiebreak (working agreement 2026-07-05), and the validator model is the illegal-states-unrepresentable one (a bound on a monotone-non-decreasing count). The required-field exclusion is the notable surprise, pinned by fixture + theorem. | matches cue v0.16.1 on every probed case (satisfied/violated, boundary, optional/required/hidden exclusion, negative/zero limit, bare-incomplete, disjunction-arm prune) | `testdata/export/struct_field_count.{cue,json}` (satisfied cases, byte-identical to cue oracle); `FixtureTests` `fieldcount_*` (24 `native_decide` — violations, boundaries, exclusions, format, disjunction-arm finalize); `testdata/wild/min-fields-disj-arm-underfill-pruned/` |
| **FIELDCOUNT-DISJ (2026-07-10, Phase-A audit fix)** — a retained field-count residual inside a disjunction ARM at manifestation | v0.16.1 | Same silence as STDLIB-STRUCT-FIELDCOUNT (stdlib validator behavior); additionally silent on the interaction with disjunction resolution — but lattice first principles decide it: unification distributes over disjunction, so `MinFields(n)` applies per arm; an arm whose struct is FINAL and under the min manifests bottom on its own; bottom arms are eliminated. | `struct.MinFields(2) & ({a:1} | {a:1,b:2})` → `{"x":{"a":1,"b":2}}` (the 1-field arm is pruned). `MinFields(1) & ({a:1} | {a:1,b:2})` → ambiguous (both satisfy). `MaxFields(2) & ({a:1} | {a:1,b:2,c:3})` → `{"x":{"a":1}}`. | **Finalize each disjunction arm at manifest.** `finalizeDisjArm` (`Kue/Manifest.lean`) maps every alternative through `finalizeLengthConj` before `resolveDisjDefault?`/`liveAlternatives` select — a violated retained bound collapses the arm to `.bottomWith`, which liveness then prunes, exactly as an in-isolation manifest would. Manifest-ONLY: meet-time `normalizeDisj` is untouched, so an under-count arm a later conjunct could still rescue (`MinFields(2) & ({a:1} | {x:9}) & {b:2}` → both arms reach 2 → ambiguous) is NOT prematurely pruned. Basis: precise + total + "eliminate bottom alternatives"; the finalize is the same manifest-time adjudication STDLIB-B already applied at the top level, now reaching nested arms. | matches cue v0.16.1 on every probed case (prune, genuine-ambiguity, max-prune, min-zero, empty-arm, accretion-preserved, min&max) | `FixtureTests` `fieldcount_disj_*` (8 `native_decide`); `testdata/wild/min-fields-disj-arm-underfill-pruned/` |
| **STDLIB-STRCONV (2026-07-10, stdlib campaign slice C)** — surface + semantics of the `strconv` package: which functions, base range, `bitSize` behavior, error shape | v0.16.1 | Silent. `strconv` is a STANDARD-LIBRARY package (mirrors Go's `strconv`), not a language construct — the spec pins none of its surface. The pure-function shape (value in, value out) follows trivially; the exact accepted set, base range, `bitSize` range semantics, and underscore rules are stdlib-package behavior the spec does not adjudicate. | Numeric parse/format is ARBITRARY-PRECISION (`Atoi("99999999999999999999999")` succeeds — cue uses `math/big`/`apd`, not int64). `ParseInt`/`ParseUint` honor base-0 prefix auto-detect (`0x`/`0b`/`0o`/leading-`0` octal), Go's underscore rule (base 0 only), case-insensitive digits, and an N-bit `bitSize` range (`0` unbounded, `b>0` signed `[-2^(b-1),2^(b-1)-1]` / unsigned `[0,2^b-1]`, `b<0` empty — NOT capped at 64 as Go's stdlib is). `FormatInt`/`FormatUint`/`ParseInt` accept base up to **62** (`math/big` leak). `strconv.Itoa` is NOT a callable function in cue v0.16.1 (calling it is a `_\|_` non-function error). | **cue-compat where unambiguous, Go-contract where cue leaks.** Shipped (`Kue/Strconv.lean`, `.strconv` `BuiltinFamily` arm): `Atoi`, `FormatInt`, `FormatUint` (= `FormatInt`), `ParseInt`, `ParseUint`, `FormatBool`, `ParseBool` — all arbitrary-precision (matches Kue's exact `Int`), with the full base-0/underscore/`bitSize` semantics above. Typed errors: `BottomReason.strconv{Syntax,Range,InvalidBase}`. **Base restricted to Go's documented `2..36`** (cue's `37..62` is a `math/big` leak — see `cue-divergences.md`). **Deferred** (route to `unsupportedBuiltin`, never a silent-wrong value): `Itoa` (non-callable in cue anyway), `FormatFloat`/`ParseFloat` (float shortest-round-trip incompatible with the exact-decimal core), `Quote`/`Unquote`/`QuoteToASCII`/… (need Go's full Unicode `IsPrint` table). Basis: spec-silent non-core surface → cue-compat tiebreak (2026-07-05), plus correctness-over-coverage (defer rather than approximate). | matches cue v0.16.1 on the shipped surface (base `2..36`, sign, prefixes, underscores, `bitSize` boundaries, bool sets); DIVERGES only on base `37..62` (Go contract, logged in `cue-divergences.md`) and on deferred functions (bottom vs cue's value/bottom) | `testdata/export/strconv_basic.{cue,json}` (byte-identical to cue oracle); `Kue/Tests/StrconvTests.lean` (52 `native_decide` — representative/boundary/error, round-trip, deferred-routing) |
| **STDLIB-PATH (2026-07-11, stdlib campaign)** — surface + OS-parameterization of the `path` package | v0.16.1 | Silent. `path` is a STANDARD-LIBRARY package (a vendored, OS-parameterized copy of Go's `path/filepath`), not a language construct — the spec pins none of its surface. The pure-function shape follows trivially; the exact function set, per-OS separator semantics, os-arg defaults, and the constant set are stdlib-package behavior the spec does not adjudicate. Notably, the CUE package exposes ONLY `Unix`/`Windows`/`Plan9` — there is NO `path.OS` field (a common misconception; `getOS` maps `runtime.GOOS` names internally but surfaces nothing). | `path.Unix`/`Windows`/`Plan9` = `"unix"`/`"windows"`/`"plan9"`. All funcs default the os arg to `unix` EXCEPT `VolumeName` (default `windows`); `ToSlash`/`FromSlash`/`SplitList` have NO default (arg required). Unix and Plan9 are behaviorally identical (separator `/`, volume-len 0); the `unixOS` fallback names (`darwin`/`linux`/…) also route to unix (`getOS` default). An os string outside the `Unix\|Windows\|Plan9\|…unixOS` disjunction is a unification error (bottom). `Match` is Go's `filepath.Match` (`^`-negated classes, `!` literal, `getEsc` errors on leading `-`/`]`, `*`/`?` never cross `/`, `\` escapes, `**` rejected). | **cue-compat (spec-silent non-core surface → cue-compat tiebreak, 2026-07-05), unix/plan9 FULLY, windows DEFERRED.** `Kue/Path.lean` (algorithms) + `.path` `BuiltinFamily` arm (`evalPathBuiltin`, `Builtin.lean`). Shipped for unix/plan9: `Clean`/`Join`/`Split`/`Dir`/`Base`/`Ext`/`IsAbs`/`SplitList`/`Resolve`/`Rel`/`Match`/`ToSlash`/`FromSlash`/`VolumeName`, each with cue's os-arg arities. **`windows` os ⇒ `unsupportedBuiltin` deferral** (a bare `VolumeName(p)` therefore defers): faithful volume-name/UNC/backslash handling is a large, error-prone corner deferred rather than shipped wrong (correctness-over-coverage). Constants resolved as `stdlibPackageValue?` entries; `path.OS` is NOT provided (not a real cue constant). Basis: illegal-states-total port (fuel-bounded `Match`, structural lexical helpers), cue-compat where the spec is silent. | matches cue v0.16.1 on the shipped unix/plan9 surface (every function, edges, os constants, plan9==unix); DIVERGES only by DEFERRING windows (bottom vs cue's value) and a bare `VolumeName` (defers vs cue's windows-default value) | `Kue/Tests/PathTests.lean` (75 `native_decide` — all functions, edges, os constants, plan9==unix, windows deferral, invalid os, bad Match pattern, `Rel` error, 3 end-to-end export) |
| **STDLIB-TIME (2026-07-11, stdlib campaign)** — surface + scope boundary of the `time` package | v0.16.1 | Silent. `time` is a STANDARD-LIBRARY package (a vendored slice of Go's `time`), not a language construct — the spec pins none of its surface. Function shapes, constant values, and the validator/predicate duality (a bool-returning builtin used as `x & pred(args)` prepends `x` as the FIRST arg) are stdlib/CUE-runtime behavior the spec does not adjudicate. | Duration UNIT constants are int nanoseconds (`Nanosecond`=1 … `Hour`=3600000000000); format-LAYOUT constants are Go reference-time strings (`RFC3339`=`"2006-01-02T15:04:05Z07:00"`, `Kitchen`=`"3:04PM"`, …); `January`=1…`December`=12, `Sunday`=0…`Saturday`=6. `ParseDuration(s)` → int64 nanoseconds, ERRORS on overflow (Go accumulates a positive int64 and negates, so max magnitude is `2^63-1`, `-2^63` unreachable). The `Duration`/`Time` validators accept the SAME strings ParseDuration/`time.Parse(RFC3339)` accept; used bare, as `()`, or via a full call returning `true`/error (never `false`). `time.Time` = `time.Parse(RFC3339Nano, s)` — CALENDAR-VALIDATING (Feb 29 rejected in a common year; Apr 31 rejected), hour 0-23/min·sec 0-59, `T`/`Z` uppercase, optional `.fraction`, offset `Z` or `±HH:MM` (offset RANGE-checked: hour ≤ 24, minute ≤ 60, both inclusive — `+24:60` accepted, `+25:00`/`+12:61` rejected). | **cue-compat (spec-silent non-core surface → cue-compat tiebreak), EXACT/STRUCTURAL surface LANDED, CIVIL-CALENDAR/EPOCH DEFERRED.** `Kue/Time.lean` (Go-duration lexer, RFC3339 calendar validator) + `.time` `BuiltinFamily` arm (`evalTimeBuiltin`, `Builtin.lean`) + `Value.stringFormat (fmt : StringFormat)` — a meet-participating string validator mirroring `stringRegex` (a GROUND non-conforming string bottoms, an ABSTRACT string RETAINS, so `string & time.Duration()` stays incomplete — reuses the isGround/finalize discipline, no abstract-fabrication). SHIPPED: `ParseDuration`; the `Duration`/`Time` validators (bare, `()`, and boolean function forms); `Format` restricted to the `RFC3339`/`RFC3339Nano` layouts; all unit/layout/month/weekday CONSTANTS. **DEFERRED with `unsupportedBuiltin`:** `Unix`, `Parse`, `FormatString`, `Split`, `FormatDuration`, and any NON-RFC3339 custom `Format` layout (all need a date↔epoch calendar engine or Go's format machinery — a large corner deferred rather than shipped wrong). `time.Date` is a nonexistent leaf → bare bottom. **Duration is deliberately int64-bounded** (overflow ⇒ bottom): a Go `time.Duration` IS int64 nanoseconds, so the bound is the type contract, not a Kue-exactness question. Fractional-nanosecond contribution is computed by EXACT integer division (`⌊frac·unit/10^n⌋`) where Go/cue uses a float64 approximation — Kue's exact value is spec-correct; cue's float64 rounding produces a genuine off-by-one on some fractional-hour inputs (DEMONSTRATED: `0.00427738455750h` = exactly 15398584407 ns, cue returns 15398584406 — see `cue-divergences.md`). | matches cue v0.16.1 on the shipped surface (ParseDuration units/signs/fractions/combos/overflow, both validators, RFC3339 calendar+leap-year+offset range cases, constants, RFC3339 `Format`); DIVERGES by (a) DEFERRING the civil-calendar/epoch functions + non-RFC3339 `Format` layouts (bottom vs cue's value), (b) exact-integer vs cue-float64 fractional ParseDuration on the rounding boundary (`cue-divergences.md`) | `Kue/Tests/TimeTests.lean` (60+ `native_decide` — ParseDuration edges, Duration/Time/Format validators incl. abstract-retention + leap-year, function forms, deferred-fn markers, end-to-end constant/validator export); `testdata/export/time_basic.cue` |

| **STDLIB-NET (2026-07-11, stdlib campaign)** — surface + scope boundary of the `net` package | v0.16.1 | Silent. `net` is a STANDARD-LIBRARY package whose IP validators call Go's `net/netip` (`ParseAddr`/`ParsePrefix` + the `Addr.Is*` classification methods) VERBATIM — the spec pins none of its surface. Address parsing (strict dotted-decimal, IPv6 `::` compression + embedded IPv4 + `%zone`), the v4/v6 distinction (`Is4` ≠ `Is6`, 4-in-6 unmapping), the class-predicate bit-tests, and the constant values are stdlib/`netip` behavior the spec does not adjudicate. | `IPv4len`=4, `IPv6len`=16. The IP validators used bare/`()`/`(s)`; `net.X(s)` returns a BOOL (invalid ⇒ `false`, NOT an error — cue's `Is*` return `false`) EXCEPT `IPCIDR` which is `(bool, error)` and ERRORS on an unparseable CIDR. `netip.ParseAddr` is strict: no leading-zero octets (`01.2.3.4` rejects), exactly 4 octets ≤ 255; IPv6 allows one `::`, ≤ 4 hex digits/group, embedded trailing IPv4, and a non-empty `%zone` (a zone is BARRED in a CIDR prefix). `IPv4`/`IPv6` keep the family distinction (`::ffff:1.2.3.4` is IPv6-only, `::1` is IPv6-only). Class predicates unmap 4-in-6 first, EXCEPT `InterfaceLocalMulticast` (v6-only, excludes 4-in-6). `GlobalUnicast` = not unspecified/loopback/multicast/link-local-unicast and (v4) not `255.255.255.255`; private v4 + ULA v6 ARE global. `FQDN` is `golang.org/x/net/idna`'s `ToASCII` (full IDNA2008: `ab--cd`/`xn--a` reject as invalid A-labels — NOT a label-rule predicate). | **cue-compat (spec-silent non-core surface → cue-compat tiebreak), IP STRING-VALIDATOR surface LANDED, PARSERS/idna DEFERRED.** `Kue/Net.lean` (a total, fuel-bounded `netip.ParseAddr`/`ParsePrefix` port + the `Addr.Is*` classification, over a `NetAddr = v4 | v6` mirroring `netip`'s z4/z6) reuses the `Value.stringFormat` mechanism — 11 new `StringFormat` variants (`netIP`/`netIPv4`/`netIPv6`/`netIPCIDR` + 7 address-class predicates), NO new `Value` constructor (keeps the parked-2B constructor pressure flat). Meet-participating like `time`'s: a GROUND non-conforming string bottoms, an ABSTRACT `.kind .string` RETAINS (no abstract-fabrication). `.net` `BuiltinFamily` arm (`evalNetBuiltin`, `Builtin.lean`); bare validators + `IPv4len`/`IPv6len` resolved as `resolveQualifiedConstant` entries (`Parse.lean`). **DEFERRED with `unsupportedBuiltin`:** `FQDN` (needs the idna engine), and every function returning a struct/list/tuple — `SplitHostPort`/`JoinHostPort`, `ToIP4`/`ToIP16`, `ParseCIDR`, `ParseIP`, `AddIP`/`AddIPCIDR`, `InCIDR`, `CompareIP`. A nonexistent leaf (`net.Host`, `net.CIDR`) → bare bottom (B-1). A non-string validator argument (a byte-list IP, which `netGetIP` accepts) also DEFERS (bottom vs cue's bool) — a documented scope gap. | matches cue v0.16.1 on the shipped surface (280 IP-class differential cases + full CIDR battery byte-identical; validators, constants, function forms, end-to-end export all IDENTICAL to cue); DIVERGES only by DEFERRING `FQDN` + the struct/list/tuple functions (bottom vs cue's value) and byte-list validator arguments | `Kue/Tests/NetTests.lean` (80+ `native_decide` — validators, family membership, abstract-retention, disj-arm survival, all 7 class predicates, CIDR boundaries, function forms, deferred-fn markers, nonexistent-leaf bottom, end-to-end export); `testdata/export/net_basic.{cue,json}` (byte-identical to cue) |
| **STDLIB-TEXTTEMPLATE-T1 (2026-07-11, stdlib campaign)** — surface, rendering shape, and scope boundary of the `text/template` package's `Execute`/`HTMLEscape`/`JSEscape` | v0.16.1 | Silent. `text/template` is a STANDARD-LIBRARY package wrapping Go's `text/template` engine — the CUE spec pins none of its surface. cue v0.16.1 exposes EXACTLY three callable leaves (`Execute(template, data) -> string`, `HTMLEscape(s) -> string`, `JSEscape(s) -> string`); every other name (`Parse`, `New`, …) is a non-function `_\|_`. The template grammar, Go-truthiness, key-sorted struct/range iteration, the `<no value>`/`<nil>` null distinction, the Go-`fmt` rendering shape (`map[k:v …]`, `[a b c]`), and the escaper replacement sets are Go-runtime behavior the spec does not adjudicate. | Struct data renders KEY-SORTED as Go `fmt` `map[a:1 b:2]`; a list as `[1 2 3]` (space-joined, NOT JSON); `range` over a struct iterates VALUES in key order; a null/missing field rendered by an action is `<no value>`, but a null NESTED inside a `map[]`/`[]` is `<nil>`; a missing field is nil (falsy, chains to nil), not an error, but a field access on a SCALAR errors. Go truthiness: empty string/0/false/nil/empty collection ⇒ false. `{{-`/`-}}` trim ` \t\r\n`. `HTMLEscape` replaces `< > & ' "` (→ `&lt; &gt; &amp; &#39; &#34;`) and NUL → U+FFFD, all else verbatim. `JSEscape` escapes `\ ' " < > & =` (→ `\\ \' \" < > & =`, uppercase hex) + control chars < 0x20 → `\u00XX`, and per `unicode.IsPrint` for non-ASCII. cue ALSO renders floats (`{{.x}}` on `1.5` → `"1.5"`) and computes the full builtin/pipeline/variable/`define` grammar. | **cue-compat (spec-silent non-core surface → cue-compat tiebreak, 2026-07-05), T1 GREEN CORE + escapers LANDED, the IsPrint/float/full-grammar tail DEFERRED (correctness-over-coverage).** `Kue/TextTemplate.lean` (leaf module, imports `Kue.Value` only) is a total, fuel-bounded lexer + parse-tree + tree-walk evaluator over its own `TemplateData` tree (float UNREPRESENTABLE by construction); `Kue.manifestToTemplateData` (`Builtin.lean`) is the `Value`→data bridge (manifests the args, KEY-SORTS struct fields, routes any float/bytes to `none`). `.textTemplate` `BuiltinFamily` arm (`evalTextTemplateBuiltin`). SHIPPED: text passthrough; `{{.F}}`/`{{.A.B}}`/`{{.}}`; `{{if}}`/`{{range}}`(list/struct/null)/`{{with}}` with `{{else}}`; `{{/* */}}`; `{{-`/`-}}`; scalar/struct/list Go-`fmt` rendering; missing-field & null ⇒ `<no value>`. **DEFERRED with `unsupportedBuiltin`** (a template USING one ⇒ `text/template.Execute` marker, never a wrong result): **any FLOAT in the data** (the shortest-round-trip `strconv.FormatFloat` kernel is the same deferred wall — T3, folded into the float campaign); all builtin FUNCS (`and`/`len`/`eq`/…), pipelines `{{a|b}}`, variables `{{$x}}`, `printf`, `{{define}}`/`{{template}}`/`{{block}}`; `range` over a scalar. `JSEscape` of a non-ASCII string ⇒ `text/template.JSEscape` marker (Go escapes per `unicode.IsPrint`, the SAME deferred Unicode table as `strconv.Quote` — deferred, never approximated). A MALFORMED template (unclosed action, stray `{{end}}`) or field-access-on-scalar ⇒ bottom; a non-concrete template/data ⇒ pending residual / bottom (mirrors `json.Marshal`); a nonexistent leaf (`template.Parse`) ⇒ bare bottom (B-1). Basis: illegal-states-total leaf engine + defer-not-approximate. | matches cue v0.16.1 on the shipped surface (35-case rendering/control/escaper differential BYTE-IDENTICAL, incl. `Execute("Hello {{ .name }}", {name:"World"})`, key-sorted map/range, `<no value>`/`<nil>`, trim, both escapers' ASCII output); DIVERGES only by DEFERRING (bottom/marker vs cue's value) — float data, the full FUNC/pipeline/var/`define` grammar, and non-ASCII `JSEscape` (all recognized-but-not-computed, a kue subset, NOT a cue bug) | `Kue/Tests/TextTemplateTests.lean` (60+ `native_decide` — rendering, if/range/with, trim, comment, missing/null, parse/eval-error bottom, deferred-construct + float + non-ASCII markers, escapers, the `manifestToTemplateData` bridge, `evalBuiltinCall` verdicts); `testdata/export/text_template_basic.{cue,json}` (byte-identical to cue) |
| **STRING-ESCAPE-SET (2026-07-11)** — the exact double-quoted string escape set + malformed-escape handling | v0.16.1 | Partially silent. The CUE spec's `escaped_char` grammar enumerates `a b f n r t v \ ' "` and the `\uNNNN`/`\UNNNNNNNN` Unicode escapes; it does NOT list `/`, yet cue accepts `\/`. The spec also does not spell out the context-sensitivity of the escapable quote (that `\'` is byte-literal-only and `\"` is string-only) — that falls out of the active-quote scanner, a cue-runtime detail. | `\/` decodes to `/` (cue leniency beyond the strict grammar). The escapable quote is context-sensitive: in a double-quoted STRING `\"` is valid and `\'` errors `unknown escape sequence`; in a single-quote BYTE literal the mirror holds (`\'` valid, `\"` errors). `\xNN`/`\NNN` octal are byte-literal-only — a parse error in a string (`invalid syntax`). Malformed escapes are ALL parse errors: unknown letters (`unknown escape sequence`), short hex (`illegal character … in escape sequence`), surrogate (`unmatched surrogate pair`), out-of-range code point (`invalid Unicode code point`). | **cue-compat (spec-silent/lenient lexer surface → cue-compat tiebreak, 2026-07-05).** `decodeStringEscape` (`Kue/Parse.lean`) whitelists `\a\b\f\n\r\t\v\\\/\"` + `\uNNNN`/`\UNNNNNNNN`; every other escape body ⇒ `none` ⇒ the caller raises `invalid escape sequence in string literal`. `\u`/`\U` values are gated on `Nat.isValidChar` (surrogate + >0x10FFFF rejected, no silent replacement). `\/` included to match cue (not in the strict grammar); `\'` rejected in strings (context-sensitive quote). kue's error MESSAGE text differs from cue's (kue's own style), the accept/reject VERDICT matches. The mirror single-quote BYTE path (`decodeByteEscape`) is now cue-strict too (BYTE-ESCAPE-STRICT closed 2026-07-11): byte set `\a\b\f\n\r\t\v\\\'\/`, `\xNN`, `\NNN` octal, `\uNNNN`/`\UNNNNNNNN` (surrogate/out-of-range rejected via `Nat.isValidChar`) decode; `\"` and unknown/malformed/short escapes are parse errors; `\(` errors as unimplemented byte interpolation. Both callers (`parseQuotedByteBody`, `parseMultilineByteBody`) raise on `none` rather than keeping the escape literally. | matches cue v0.16.1 on BOTH the double-quoted string AND the single-quote byte verdict (every simple/Unicode/byte escape decoded byte-identical; every unknown/malformed/surrogate/out-of-range case rejected; `\"` rejected in bytes, `\'` rejected in strings — context-sensitive quote) | `Kue/Tests/ParseTests.lean` `string_escape_*` (19) + `byte_escape_*` (18 `native_decide`); `Kue/Tests/BytesTests.lean` `lex_bytes_*`; wild fixture `testdata/wild/cue-unicode-escape-dropped/` |
| **STDLIB-FLOAT-F0 (2026-07-11, float campaign)** — `math` log/exp domain errors + the constant/function precision surface | v0.16.1 | Silent. `math` is a STANDARD-LIBRARY package wrapping Go's `math` + cockroachdb `apd`; the CUE spec pins none of its surface. The log/exp precision (apd 34-significant-digit context), the constant values (60–64-digit `big.Float`), and the out-of-domain behavior (`Log(0)`, `Log(neg)`) are stdlib/apd artifacts the spec does not adjudicate. | Constants are 60–64-digit apd decimals (`Pi`, `E`, `Phi`, `Sqrt2`, `SqrtE`, `SqrtPi`, `SqrtPhi`, `Ln2`, `Log2E`, `Ln10`, `Log10E` — exactly eleven; no `MaxInt`/`Inf`/`NaN`). `Log`/`Log2`/`Log10`/`Exp`/`Exp2` compute in the apd 34-sig-digit context (`Log(2)=0.6931…1766`), keeping a SIGNIFICANT trailing zero within the 34 (`Log10(2)=…244930`) and collapsing a truly-integral result to `int` (`Log2(8)=3`). `Log1p`/`Expm1` are FLOAT64 (17-digit), NOT apd. Out of domain: `Log(neg)` errors `invalid operation` (bottom); `Log(0)` yields `-Inf`, which cue then FAILS to render (`invalid character 'I' in numeric literal`) — an unrepresentable-value error. | **cue-compat (spec-silent non-core surface → cue-compat tiebreak, 2026-07-05).** The existing `decimalLnScaled`/`decimalExpScaled` kernels (already backing `math.Pow`'s general domain) wire to `math.Log`/`Log2`/`Log10`/`Exp`/`Exp2` via a shared `renderTranscendentalScaled` (rounds to 34 sig digits, keeps significant trailing zeros, collapses only truly-integral results). Constants are hardcoded apd literal strings (`stdlibPackageValue?`), round-tripping byte-identically through `renderFloatText`. **DOMAIN choice: `Log`/`Log2`/`Log10` of a NON-POSITIVE argument → `.bottom`.** Kue has NO `Inf`/`NaN` (exact-decimal by design), so both `Log(0)` (`-Inf`) and `Log(neg)` (`invalid operation`) collapse to bottom — cue errors on both anyway (0 via render failure, neg via invalid-operation), so the VERDICT matches even though the mechanism differs. `Log1p`/`Expm1` stay `unsupportedBuiltin` (float64, deferred to the float campaign, never approximated by apd). | matches cue v0.16.1 on the shipped surface (all 11 constants + Log/Log2/Log10/Exp/Exp2 byte-identical incl. trailing-zero cases + int-collapse; both domain-error inputs bottom, matching cue's error verdict); DIVERGES only by DEFERRING `Log1p`/`Expm1` (float64) with `unsupportedBuiltin` | `Kue/Tests/BuiltinTests.lean` `math_log_*`/`math_exp_*`/`math_log2_*`/`math_log10_*` + `math_constants_and_transcendentals_export_end_to_end` + `math_pow_general_keeps_significant_trailing_zero`; `testdata/cue/builtins/math_log.{cue,expected}` |
| **STDLIB-FLOAT-F4 (2026-07-11 `+ - *`; 2026-07-12 `/`, float campaign)** — the apd RESULT exponent float `+ - * /` carry, which fixes the rendered form | v0.16.1 | Silent on float DISPLAY (same basis as GDA-FLOAT-RENDER, which pinned LITERAL rendering). The spec pins float VALUE semantics but not the textual form an arithmetic RESULT renders to. `cue` follows General Decimal Arithmetic (apd / IEEE-754-2008 decimal): each op has an IDEAL result exponent that drives the rendered form (scientific-vs-plain threshold, trailing zeros, `.0`). Kue's normalized `DecimalValue` (non-negative scale) collapses a positive exponent into the coefficient, erasing it. | apd IDEAL exponent per op: **add/sub** = `min(e₁, e₂)`; **multiply** = `e₁ + e₂`. The exact result is then rounded HALF-UP to the apd context precision (34 significant digits — `1e34 + 1 = 1.000…e+34`; `1e33 + 1` stays exact at 34 digits). A zero add/sub result KEEPS the ideal exponent (`1e1 - 1e1 = 0e+1`). Trailing zeros survive as coefficient magnitude (`1.20 + 1.30 = 2.50`, `1.0 * 1.0 = 1.00`). Rounding mode is HALF-UP (away from zero), NOT apd's nominal HALF-EVEN — empirically verified: `10000000000000000000000000000000005 + 0 = 1.000…001e+34` (the even-tie rounds UP). **Division's form depends ONLY on the exact quotient VALUE** (verified: every operand-exponent spelling of a value renders identically), pinned via `cue export --out json` (JSON's empty whole-tail disambiguates a real trailing `.0` from an integer's style suffix). | **cue-compat (spec-silent form surface → cue-compat tiebreak). `+ - * /` LANDED.** Float `+ - *` thread the apd `(sign, coefficient, exponent)` form (`ApdForm` + `apdAdd`/`apdSub`/`apdMul` + `apdRoundToContext`, `Decimal.lean`), extracting each operand's exponent from its render-anchor `text` (an int is exponent 0) and emitting the result as a canonical carrier `text` (`[-]coefficient e exponent`) that `floatApdForm` round-trips exactly, so every output style derives correctly and `parseDecimalText` recovers the exact `DecimalValue`. **Division** (`apdDivide?`, `Decimal.lean`) does the same for an EXACT-TERMINATING quotient. For the minimal value `±m · 10^k` (`m` trailing-zero-free, `d = digits(m)`): an integer value (`k ≥ 0`) whose adjusted exponent `k + d − 1 ≤ 32` (= apd precision 34 − 2) gains ONE trailing zero — `(10·m, k − 1)`, forcing the `.0`/`X.0e+n` float form (`6e2/3 → 2.0e+2`, `8/2 → 4.0`, `10/1 → 10.0`, `1000000/8 → 1.250e+5`); at adjusted exponent 33 the cap keeps the minimal form (`1e34/1 → 1e+34`, `1e33/1 → 1e+33`), as does a fractional/negative-scientific value (`1/4 → 0.25`, `25/1e8 → 2.5e-7`). A zero numerator keeps the ideal exponent `e₁ − e₂` clamped to `0` (renders `0`/`0.0`) when `≥ 0`, else `−1` (`0/3 → 0`, `0e2/8e3 → 0.0`). Termination is detected by removing 2s/5s from the gcd-reduced denominator; a non-terminating OR >34-significant-digit quotient falls to the unchanged 34-digit `divideDecimalRational?` renderer, which already re-parses through the apd render anchor correctly. NOTE: the follow-up's earlier DERIVED rule (`e_r ≥ 0 ⇒ (C_r·10, e_r−1)` unconditionally) was INCOMPLETE — it missed the adjusted-exponent-33 cap and the zero clamp, surfaced by `cue export --out json` (eval's whole-tail hid the `(m,0)` vs `(10m,k−1)` distinction); the implemented rule above is the corrected form, validated on 3000+ random cases. | `+ - * /` byte-identical to cue v0.16.1 (scientific/trailing-zero/zero-exp/34-digit-rounding, incl. `1e100 + 1`; division across scientific/plain/cap/zero, 600 random export-json cases + 300 yaml identical) | `testdata/wild/float-apd-exponent-preservation/` (`+ - *`) + `testdata/wild/float-apd-division-exponent/` (`/`); `testdata/cue/numeric/float_apd_arithmetic.{cue,expected}`; `EvalTests` `eval_add_sub_scientific`/`eval_add_trailing_zeros`/`eval_add_context_rounding`/`eval_div_*`; `FloatTests` `float_div_apd_ideal_exponent` |
