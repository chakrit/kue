# CUE divergences

Cases where Kue's output **intentionally differs** from the reference `cue` binary
because `cue` is buggy, surprising, or under-specified and Kue does the correct thing.
This is a living reference: the continuous slice loop appends an entry whenever a slice's
oracle-check against `cue` surfaces a genuine divergence (see `CLAUDE.md` →
"Continuous slice loop").

This is **not** for behavior we haven't implemented yet (that's the plan's Later Slices)
nor for fixtures where Kue matches `cue` (the default — those need no entry). Only record
deliberate, defensible disagreement with the reference binary.

## How to record an entry

When a slice finds `cue` doing the wrong thing:

1. Confirm it is a real `cue` defect, not a Kue bug or a misread spec — check the CUE
   language spec and, where useful, the upstream `cue` issue tracker.
2. If Kue's behavior is the corrected one, **do not** edit the fixture `.expected` to match
   buggy `cue`. Encode the correct value, and add an entry below.
3. Keep the fixture pair as the executable record; this table is the human-readable index.

## Entry format

| Topic | `cue` ver | Claim / input | `cue` output | Kue output | Why Kue is right | Fixture |
|-------|-----------|---------------|--------------|------------|------------------|---------|

## Confirmed divergences

| Topic | `cue` ver | Claim / input | `cue` output | Kue output | Why Kue is right | Fixture |
|-------|-----------|---------------|--------------|------------|------------------|---------|
| `export -e` from stdin | v0.16.1 | `echo 'common: {name: "svc"}' \| <bin> export -e common` | `reference "common" not found` (exit 1) | `{"name": "svc"}` (exit 0) | The field `common` is present in the piped document, so selecting it must succeed. `cue` ignores stdin when `-e/--expression` is given — it resolves the expression against an empty scope, so every stdin selector fails; the same `-e common` works against identical content as a file. Kue binds the selector against the actually-loaded root in both file and stdin mode (parity). | CLI behavior; `Kue/CliTests.lean` stdin-`-e` cases |
| residual pattern in `eval` output | v0.16.1 | `eval` of `{[=~"x"]: string, xy: "hi"}` | `{xy: "hi"}` — the `[=~"x"]: string` pattern constraint is ELIDED from the printed value | `{xy: "hi", [=~"x"]: string}` — the residual pattern is shown | The pattern constraint is part of the struct's value (it still constrains any future field added by a meet), so printing it is the faithful rendering; `cue eval` drops it, hiding a live constraint. Values agree (cue applies the pattern — `xy: "hi"`, a conflicting one bottoms the field); only the surface syntax differs. Concrete `export`/JSON output (the primary observable) is identical, so no fixture drifts. | `Kue/Tests/EvalTests.lean` `eval_pattern_struct_*` |
| arithmetic negative zero (GDA-FLOAT-RENDER) | v0.16.1 | `export` of `a: 0.0 * -1` | `-0.0` — apd multiplication produces a signed zero that `cue` renders on export | `0.0` — negative zero normalizes to `0.0` on render | `-0.0 == 0.0` is already true in Kue (float equality is by value), so rendering two equal values differently by arithmetic provenance is internally inconsistent. `cue` ITSELF normalizes a *literal* `-0.0` to `0.0` at parse (`export -0.0` → `0.0`) — Kue merely extends that normalization consistently to the arithmetic path (`renderFloatText` drops the sign of a zero coefficient). Negative zero is a float64 wart with no place in an exact-decimal model; the spec pins no signed-zero semantics. Kue's own arithmetic already yields `0.0` for `0.0 * -1` (only the render normalization is new); the literal `-0.0`→`0.0` case MATCHES cue. | `FloatTests` `float_render_json` (`-0.0`→`0.0`); `float_apd_form_faithful` (`-0.0`→coeff 0, sign dropped) |
| nested closedness shed on instantiation (SC-2b) | v0.16.1 | `#D: {r: {x: int}}` ; `(#D & {}).r & {x: 1, extra: 2}` | `{x: 1, extra: 2}` — `extra` ADMITTED (the closed `r` re-opened) | `{x: 1, extra: _|_}` — `extra` REJECTED (closedness preserved) | Spec: referencing a def recursively closes it "anywhere within the definition"; closedness is MONOTONE through meet (`&` cannot remove a constraint). cue is internally inconsistent — the DIRECT path `#D.r & {x, extra}` REJECTS `extra` (cue+Kue agree), but inserting a no-op `& {}` instantiation re-opens it. The `& {}` meets with the top struct, which is identity on closedness, so it cannot lattice-logically add openness — cue's re-open is an eval-strategy artifact. Kue preserves closedness on BOTH paths (SC-2's closing field-walker twin closes the nested value once; meet then carries it through monotonically). | `definitions/sc2b_instantiated_def_field_stays_closed` |
| nested closedness shed via a hidden-field binding (SC-4) | v0.16.1 | `#A: {_h: {x: 5}}` ; `y: #A` ; `out: y._h & {extra: 2}` (the bound-then-select path) | `{extra: 2, x: 5}` — `extra` ADMITTED (the nested `_h` re-opened after selecting through the regular binding `y`) | `{x: 5, extra: _|_}` — `extra` REJECTED (closedness preserved) | Same MONOTONE-closedness basis and SAME eval-strategy-artifact CLASS as SC-2b. cue is internally inconsistent: the DIRECT-MEET path `#A & {_h: {x: 5, extra}}` and the DIRECT-SELECT path `#A._h & {extra}` BOTH REJECT `extra` (cue+Kue agree); only routing the selection through a regular binding `y: #A` re-opens the nested hidden value — closedness lost crossing a plain field, an eval artifact (the `_h` value carries no `...`, so it is a closed struct on every spelling). The SC-4 fix routes a def's nested HIDDEN and LET values through the closing field-walker (like regular fields), so Kue closes them MONOTONICALLY regardless of selection path; the direct paths now match cue, only the bound-select re-open diverges. | `definitions/sc4_hidden_nested_closes` (direct meet, cue agrees); pins `EvalTests` `eval_sc4_hidden_field_nested_closes` + `eval_sc4_let_read_nested_closes` |
| `__`-prefixed field label via the inline shorthand | v0.16.1 | `a: __x: 1` (inline nested-field shorthand) | `{"a": {}}` — ACCEPTED (the `__x` field is silently admitted, treated like a hidden field) | parse error `identifiers starting with '__' are reserved: '__x'` — REJECTED | Spec: "CUE reserves all identifiers starting with `__` (double underscores) as keywords." A reserved keyword is not a valid identifier in ANY position, so a `__`-prefixed field label is spec-invalid everywhere. cue is internally INCONSISTENT — it rejects `__x: 1` (top-level), `a: { __x: 1 }` (brace form), and every `__x` REFERENCE with exactly this "reserved" error, but the single-line inline-field shorthand `a: __x: 1` slips past the check (a parser inconsistency). Kue enforces the reservation at the one identifier-lexing chokepoint (`parseIdentifier`), so `__x` is rejected on every spelling, conforming to the spec; only the inline shorthand differs from cue. `#__x`/`_#__x` (definition prefixes) and quoted `"__x"` (a string label, not an identifier) are NOT reserved and stay valid on both. | parse pins `ParseTests` `parse_double_underscore_inline_nested_reserved` + `parse_double_underscore_*` |
| Deferred-comprehension / deferred-dynamic-field eval display (D#1b, DYN-DEF-1) | v0.16.1 | `eval` of `x: bool` ; `out: {if x {a: 1}}` (incomplete guard); ALSO `kind: string` ; `out: {(kind): "m"}` (incomplete dynamic-field KEY, DYN-DEF-1) | HOLDS with the SOURCE name: `out: {if x {a: 1}}` / `out: {(kind): "m"}` | HOLDS it too, but renders the resolved ref by frame slot: `out: {if @1.0 {a: 1}}` / `out: {(@0.0): "m"}` | The VALUE verdict agrees — both HOLD the residual (cue eval-holds; Kue keeps a residual `.structComp` carrying the comprehension OR the `.dynamicField`), and both surface it as an incompleteness under `export` (cue `incomplete bool` / `key value of dynamic field must be concrete`, Kue `incomplete value: …`). DYN-DEF-1 fixed the prior DROP (the abstract-keyed dynamic field vanished entirely, exporting `{}`); it now defers exactly like an incomplete guard. Only the residual's variable RENDERING differs: Kue's `Resolve` pass rewrites names to `BindingId`s before eval, so a re-emitted residual has no name to print and `Format` shows `@depth.index`. Same display-only family as the "residual pattern in eval output" and D#2a/D#2b rows; concrete `export` output (the primary observable) carries no held residual at all (it errors), so no JSON fixture drifts. | `comprehensions/guard_incomplete_defers`; `definitions/dyndef_dynfield_rekeyed_by_narrowing` (re-key); `ComprehensionTests` `dyndef_*` / `PresenceTests` `classify_dynlabel_*` |
| Unreferenced comprehension `let` clause that fails to resolve (D#3) | v0.16.1 | `out: [for x in [1,2] let unused = someUndef {v: x}]` — a `let` clause whose value is an UNRESOLVED reference (`someUndef`), never read by the body; also self-ref `let y = y` | ERRORS (`reference "someUndef" not found`; `let y = y` → `reference "y" not found`) even though `unused`/`y` is never selected | TOLERATES — `out: [{v: 1}, {v: 2}]`; `let y = y` resolves as a reference cycle (`y → _`, the binding `_`, body unaffected) | A `let` clause nothing in the comprehension references is dead code — it contributes nothing to the result. Erroring on a never-selected binding makes a value's denotation depend on the presence of unused declarations, which is not lattice-meaningful. Kue binds the `let` value into a frame slot and evaluates it ONLY when a `.refId` selects it (the lazy-on-reference path every frame binding uses); an unreferenced slot is never forced, so its unresolved-ref / self-ref bottom never surfaces. The *referenced* case `{v: unused}` DOES error in both (cue + Kue agree). The divergence is confined to the dead-binding case; the eval-order *basis* (eager-into-frame; a value-level bottom in an unreferenced let likewise does not propagate — there cue AGREES) is recorded in `cue-spec-gaps.md`. | `comprehensions/list_let_*` (referenced + headline cases); pin `ComprehensionTests` `letcomp_unreferenced_bottom_drops` (value-bottom twin, cue agrees) |
| Malformed import-path `:identifier` qualifier (F-3) | v0.16.1 | `import "domain.com/pkg:2bad"` (and `:#foo`, `:` empty) — a qualifier that is NOT a valid CUE identifier per the spec grammar `ImportPath = '"' ImportLocation [ ":" identifier ] '"'` | PARSES the junk qualifier, deferring to a *load*-time failure: `cannot find module providing package domain.com/pkg:2bad` (empty `:` → `cannot determine package name … set it explicitly with ':'`). The parser accepts any suffix text. | REJECTS at PARSE time: `invalid package identifier in import path: '2bad'`. `splitImportPath`/`isPackageIdentifier` enforce the identifier rule (non-empty, identifier-start then identifier-parts, not a `#`/`_#` definition identifier, not the lone blank `_`) and a non-empty ImportLocation when the qualifier is split out of the location. The lone-`_` and empty-location cases cue ALSO rejects (cue: `_ is not a valid import path qualifier`; `invalid import path: ":foo"`) — same verdict, just cue surfaces them at load/path-validation rather than parse. | The spec grammar mandates `[ ":" identifier ]` — a leading-digit/empty/definition-id suffix is not an `identifier` production, so it is a syntax error, not a resolution miss. cue's acceptance is a parser permissiveness artifact that surfaces the defect only at module-resolution time (and never for a builtin/in-tree path with the right shape). Rejecting at parse is illegal-states-unrepresentable: a malformed qualifier never reaches the loader. WELL-FORMED qualifiers (`:math`, `:_foo`) parse and resolve identically in both. | `modules/qualified_import_invalid_id` (`expected.err`); pins `ParseTests` `parse_import_*_qualifier_errors` |
| Embedded pure-pattern closed def drops its closedness AND constraint (EMBED-CLOSE-1) | v0.16.1 | `#A: {[=~"^x"]: int}` ; `out: {#A, y1: 5}` — embed a closed def whose ONLY member is a pattern, into a sibling struct adding a non-matching field. Spec-equivalent to the meet form `#A & {y1: 5}`. | ADMITS `y1` (`{"out": {"y1": 5}}`), and even admits `y1: "str"` — a STRING where `#A`'s pattern says `int`. cue drops BOTH `#A`'s closedness and its pattern value-constraint when `#A` is embedded with no regular field of its own. The meet form `#A & {y1: 5}` REJECTS (`field not allowed`) — cue contradicts itself between the two spec-equivalent spellings. | REJECTS `y1` (`_\|_`, `field not allowed`), identically to its own `#A & {y1}` meet form. | Embedding conjoins the embedded value with its sibling declarations: `{#A, y1: 5}` ≡ `#A & {y1: 5}` (spec: embedding "is unified with the struct in which it is embedded"). `#A` is closed to labels matching `^x`; `y1` matches none, so it is `field not allowed`, and `y1`'s value must satisfy the `int` pattern. cue admitting `y1` (worse: `y1: "str"`) drops a live constraint purely because the def is pattern-only and embedded — an eval-strategy artifact, the same internal-inconsistency class as SC-2b (cue agrees on one spelling, re-opens on the other). Kue applies closedness MONOTONICALLY regardless of spelling. PRE-EXISTING (reproduces on the `f0613e5` baseline, not introduced by SC-1b); a field-bearing closed def `#A:{a:int}` embedded already rejects in BOTH. | PINNED 2026-06-21 (SC-1e slice): `StructTests ### EMBED-CLOSE-1` (`embed_close1_{meet,embed}_form_rejects`) + fixture `definitions/embed_close1_pin` lock kue's correct rejection of BOTH spellings against regression. |
| `math.Sqrt` exact decimal vs `cue`'s float64 (BI-2-residual) | v0.16.1 | `math.Sqrt(2)`, `math.Sqrt(100)`, `math.Sqrt(1000000)` | `1.4142135623730951` (IEEE-754 float64 — only 17 digits); `1e+1`; `1e+3` (Go scientific-notation float formatter). And `cue`'s own `math.Pow(2, 0.5) = 1.414213562373095048801688724209698` DISAGREES with its `Sqrt(2)` — `cue` computes `Sqrt` in float64 but `Pow` in apd decimal, so `Sqrt(x) ≠ Pow(x, ½)` inside `cue`. | `1.414213562373095048801688724209698` (34 significant digits — byte-identical to `cue`'s OWN `Pow(2, 0.5)`); `10`; `1000`. Kue computes `Sqrt` in exact decimal via `decimalSqrt` (fixed-iteration integer-Newton root, perfect squares collapsed to `int`), and routes `Pow(x, ½)` through the SAME function, so `Sqrt(x) = Pow(x, ½)` holds. | Kue is MORE precise (34 vs 17 digits) AND self-consistent. An exact-rational system has no float64 and no scientific-notation float formatter, so it neither truncates to 17 digits nor renders `1e+1`. `cue`'s `Sqrt ≠ Pow(·,½)` is an internal inconsistency (two different numeric libraries for the same mathematical operation); Kue removes it. The spec does not pin a precision model for `Sqrt`/`Pow` (library artifact — see `cue-spec-gaps.md`), so matching `cue`'s `Pow` apd context and extending it to `Sqrt` is the principled, illegal-states-fewer choice. | `builtins/math_sqrt` (`sqrtTwo`/`sqrtHundred`/`sqrtBigSq`/`powHalf`); pins `BuiltinTests` `math_sqrt_*` / `math_pow_half_*` / `math_sqrt_equals_pow_half` |
| `Sqrt(neg)` / `Pow(0, neg)` real-domain errors → bottom, not `NaN`/`Infinity` (BI-2-residual) | v0.16.1 | `math.Sqrt(-1)`; `math.Pow(-2, 0.5)`; `math.Pow(0, -1)` | `NaN.0`; `error in call to math.Pow: invalid operation` (cue DOES error this one); `Infinity` — `cue` manufactures the IEEE float specials `NaN`/`Infinity` as ordinary values. | `_\|_` (bottom) for all three — `math.Sqrt(-1)` and `Pow(-2, ½)` are real-domain errors (√ of a negative / a complex root), and `Pow(0, neg)` is a division by zero. | An exact-rational lattice has no `NaN`/`Infinity` value to manufacture — those are float-IEEE artifacts, not lattice elements. `√(neg)` leaves the real numbers (complex) and `0^(neg) = 1/0^|neg|` divides by zero; both are genuine domain errors, so bottom is the illegal-states-cleaner verdict (`cue` already agrees on `Pow(-2, 0.5)`, erroring it — it is internally inconsistent in emitting `NaN`/`Infinity` for the structurally-identical `Sqrt(-1)`/`Pow(0,-1)`). Kue does not pollute the value lattice with non-lattice specials. | pins `BuiltinTests` `math_sqrt_negative_is_bottom` / `math_pow_negative_base_half_exponent_is_bottom`; `builtins/math_pow` (`powZeroNeg`/`powNegBaseFr`) |
| General `math.Pow` (neg-int + non-½ fractional) exact decimal vs cue rendering (BI-2-§3) | v0.16.1 | `math.Pow(2, -3)`; `math.Pow(3, -1)`; `math.Pow(2, 0.25)`; tiny/sci cases e.g. `math.Pow(0.0001, 0.3)` rendered `…e-7` by cue. MANTISSA is byte-identical to cue's apd throughout (`Pow(2, 0.25) = 1.189207115002721066717499970560476`, `Pow(2, 0.1) = …023342`). | `0.1250000000000000000000000000000000` (apd pads to 34 fractional places); `0.3333333333333333333333333333333333` (cue agrees on the 34 threes); `1.189207115002721066717499970560476` (agrees); and Go's scientific-notation formatter for small magnitudes (`…e-7`). | `0.125` (exact rational, trimmed); `0.3333…3333` (34 sig digits, agrees); `1.189207115002721066717499970560476` (agrees, byte-identical); plain non-scientific decimal for small magnitudes. Exact-integer results collapse to `int` (`Pow(4, 1.5) = 8`, `Pow(8, ⅓) = 2`). | The transcendental DIGITS agree with cue exactly (Kue's `decimalExpScaled`/`decimalLnScaled` match cue's apd `Pow` to 34 significant digits across the corpus — verified on 40 random + extreme-magnitude cases, mantissa-identical). The only divergences are SAME-FAMILY display artifacts already recorded for `Sqrt`: (a) cue's apd PADS terminating/short expansions with trailing zeros to a fixed width; Kue trims to the exact minimal value (an exact-rational system has no fixed-width float). (b) cue uses Go's scientific-notation float formatter for small/large magnitudes (`…e-7`); Kue renders plain decimals (the same choice as `Sqrt(100) = 10` not `1e+1`). Neither changes the value. Negative-INTEGER exponents are computed as the EXACT rational `1/x^n` (not the transcendental path), so they are exact, not merely 34-digit. | `builtins/math_pow` (`powNegInt`/`powNegRep`/`powQuarter`/`powTenth`/`powThreeHalf`/`powCubeRoot`); pins `BuiltinTests` `math_pow_negative_integer_*` / `math_pow_general_fractional_*` |
| Closed-import-def reject masked by incompleteness — error *message* differs, value AGREES (import-eager-closedness) | v0.16.1 | An imported closed def with an ABSTRACT field, met with an undeclared extra: `c: lib.#Closed` (`#Closed: {port: int, host: string}`) ; `out: c & {extra: "x"}`. BOTH the closedness violation (`extra` not allowed) and an incompleteness (`int`/`string` non-concrete) are present. | `out.extra: field not allowed` — cue surfaces the CLOSEDNESS error. | `incomplete value: int` — Kue surfaces the INCOMPLETENESS error. Both BOTTOM (exit 1); the field `extra` carries `.fieldNotAllowed` in the value either way — the disagreement is only WHICH bottom the manifest reports first. | The VALUE verdict agrees: `extra` is rejected (`.fieldNotAllowed`), the config does not export. Only the error *selection* differs — cue reports the closedness violation, Kue reports the incompleteness it reaches first in evaluation order. With a CONCRETE def (`#Closed: {port: 8080, host: "h"}`) the incompleteness vanishes and both report the closedness rejection identically. This is the same display-only family as the deferred-comprehension and residual-pattern rows: same value, different surface diagnostic. (The pre-fix behavior was a genuine bug — Kue SILENTLY ADMITTED `extra` because the eager selector left the imported def body open; the fix closes it, so the value now agrees with cue.) | pins `ClosureTests` `eager_closed_import_def_rejects_extra_when_abstract` (value-level: `extra` is `.fieldNotAllowed`); `eager_closed_import_def_rejects_extra` (concrete twin, full agreement) |
| Unknown / unimplemented builtin name — generic vs name-specific bottom message, value AGREES (TL-1) | v0.16.1 | A call to a name that is not a known builtin, with CONCRETE args: an unknown package `out: foobar.Baz("a")`, an unqualified non-builtin `out: nosuchfn("a")`, or the real-but-unimplemented `out: error("boom")`. | A NAME-SPECIFIC resolution error: `reference "foobar" not found` / `reference "nosuchfn" not found`; an unknown LEAF of a known package (`math.NoSuch(1)`) → `cannot call non-function math.NoSuch (type _\|_)`; `error("boom")` → `boom` (its message). All BOTTOM (exit 1). | `conflicting values (bottom)` — a GENERIC bottom (exit 1). The VALUE is bottom in every case, agreeing with cue's reject verdict; only the diagnostic text differs (Kue carries no name-specific "not found" / custom `error` message). | The VALUE verdict agrees — both reject (bottom), the config does not export. An unresolved builtin name is bottom per lattice first principles (an unknown function is not a value), and cue concurs on the bottom verdict; the divergence is only the surface diagnostic. PRE-FIX this was a genuine bug: the stringly-typed `startsWith` dispatch fell through to a SILENT `.builtinCall` residual, so the unknown name manifested as `incomplete value: …` (a non-error incompleteness) instead of bottom — masking the resolution error. TL-1's exhaustive `BuiltinFamily` match routes the non-builtin (`none`) case through `unresolvedOrBottom`, so concrete args now bottom; abstract args still defer (a later pass may concretise). Same display-only family as the import-eager-closedness row above: same value, different message. (Kue does not yet implement `error`'s custom-message carrier — out of scope; bottom-vs-incomplete is the soundness correction.) | pins `BuiltinTests` `unknown_family_concrete_args_is_bottom` / `unknown_unqualified_name_concrete_args_is_bottom` / `unknown_error_builtin_concrete_arg_is_bottom` / `unknown_family_abstract_arg_stays_unresolved` |
| Missing-field VALUE use — generic vs `undefined field` bottom message, value AGREES (missing-field-selection) | v0.16.1 | A genuinely-missing field of a concrete struct used as a VALUE (not a presence-test): `x: {a: 1}` ; `y: x.b`. | `y: undefined field: b` (exit 1) — cue's name-specific missing-field error. | `conflicting values (bottom)` (exit 1) — the field selects to `.bottom` (absent), which surfaces as a generic bottom. | The VALUE verdict AGREES — both reject (bottom), the config does not export. Selecting a field absent from a concrete struct is absence (`.bottom`), and the PRESENCE-test observable (`x.b == _|_` true / `!= _|_` false) now matches cue byte-for-byte; only the surface diagnostic on a value-USE differs (Kue carries no name-specific "undefined field" message, just the absent bottom). PRE-FIX this was a genuine bug: the miss deferred to a `.selector` residual classified `.incomplete`, so the presence-test stayed unresolved and `export` errored `incomplete value` — the WRONG verdict (cue says absent). The fix (`selectFromDecls` miss arm → `.bottom`) corrects the presence-test to agree with cue; the value-use message divergence is the same display-only family as the import-eager-closedness and TL-1 rows. | pins `Bug2xTests` `mfs_*` (presence-test polarities, oracle-confirmed); fixtures `export/mfs_*` |
| Mutual-recursion closed def rejects its OWN declared field (Bug2-12 MUTUAL) | v0.16.1 | A CLOSED mutual cycle: `#A: #B & {a: 1}` ; `#B: #A & {b: 2}` ; `out: #A` (and `#A & {a: 1, b: 2}`). | REJECTS even the def's OWN declared field: `#A.a: field not allowed`, `#B.b: field not allowed`, `out.a: field not allowed` (exit 1) — `#A` is read as a use-site `{a}` added to an already-closed `#B`. | ADMITS the transitively-declared fields: `out: {a: 1, b: 2}` (exit 0). A genuine extra `c` ∉ {a,b} is REJECTED (`out: #A & {c: 3}` bottoms `c`) — the closed allowed-set is the transitive UNION `{a,b}`, no more, no less. | A definition's closed allowed-set is the TRANSITIVE union of all mutually-reachable cycle members' declared labels (`#A = #B & {a} = #A & {a,b}` ⟹ `allowed(#A) = {a,b}`). Closedness BOUNDS the additions a use-site may meet in; it never rejects a label the definition itself declares — a def rejecting its OWN field contradicts the closedness invariant. cue's reading closes `#B` PREMATURELY mid-cycle (before `#A`'s back-contribution settles), so `#A`'s `{a}` is mis-read as a use-site add to a finished `#B`; this is internally inconsistent with cue's correct ACYCLIC behavior, where a def's declared field is never rejected. Kue computes the fixed-point allowed-set across the cycle (`defSlotInClosedCycle` gates the `flattenConjDefRef` close-once union over every cycle member's literals), so it ADMITS the declared/transitive fields and REJECTS only genuine extras — the lattice-principled bound. (Pre-fix Kue UNDER-CLOSED, admitting `c` too: the cross-def back-ref bottomed `#B` via the D#2 structural-cycle path, dropping its closedness and leaving `#B & {a}` OPEN. The under-close is the bug Kue FIXED; cue's over-reject is the bug Kue declines to replicate.) | pins `Bug2xTests` `bug212_mutual_*` (admit transitive `{a,b}`, reject extra `c`, base, 3-way, open-tail, one-way-non-rec boundary); fixtures `definitions/bug212_mutual_*` |
| Import-name redeclaration — single-line vs two-line diagnostic, verdict AGREES (A2-y) | v0.16.1 | A top-level bare-identifier field reusing an import's bound local name: `import "ex.com/dep"` + `dep: {x: 1}` (also `dep?`/`dep!`, the alias name under `import d "…"`, the qualifier name under `"…:foo"`, and a builtin `import "encoding/json"` + `json: {…}`). | Two lines: `dep redeclared as imported package name` + `	previous declaration at ./main.cue:5:1:` then the import-clause location. Exit 1, at LOAD. | `dep redeclared as imported package name` — the SAME first line, a LOAD error (exit 1). Kue carries no source-position back-reference (`previous declaration at …`), so the second line is absent. | The VALUE verdict AGREES — both reject at load, the config does not compile. This is spec-mandated, NOT a cue bug (the spec's "No identifier may be declared twice in the same block": the import binds `dep` in the file block, the field re-declares it), so Kue MATCHES cue's verdict and primary message; only the position-detail second line differs (Kue's loader operates on a parsed value without per-decl source spans). Same display-only family as the TL-1 / import-eager-closedness rows: same verdict, leaner diagnostic. Pre-fix this was a genuine soundness bug — Kue SILENTLY KEPT BOTH the import binding and the field, and worse, resolved a reference `out: dep` to the IMPORTED package rather than the user's field (wrong value). The exemptions (quoted `"dep"`, `#dep`/`_dep`, nested, alias/qualifier mismatch) match cue exactly. | pins `ModuleTests` (A2-y boundary: `bareIdentifierLabels` eligibility + `checkImportRedeclaration`/`checkBuiltinImportRedeclarations`); fixtures `modules/import_name_redeclaration` (`expected.err`) + `modules/import_name_no_collision` (exemptions load) |

| `math.Mod` exact decimal vs cue's float64 (BI-3-RESIDUAL) | v0.16.1 | `math.Mod(5.5, 2.1)` (also any dividend/divisor whose exact remainder is not a float64-representable decimal). | `1.2999999999999998` — cue computes Go `math.Mod` in float64, so the exact remainder `1.3` prints as its nearest-float64 artifact. | `1.3` — Kue computes `x - trunc(x/y)·y` in EXACT decimal (scale to a common denominator, truncated-toward-zero integer quotient), so a finite-decimal dividend/divisor yields the exact finite-decimal remainder. | Kue is MORE precise and internally consistent — same posture as the `math.Sqrt`/`Pow` rows. An exact-rational system has no float64, so it never rounds the remainder to a 17-digit binary artifact. The spec pins `math.Mod` to Go's float-remainder *identity* (sign of the dividend, `x - trunc(x/y)·y`) but not a float64 precision model (library artifact — see `cue-spec-gaps.md`'s Sqrt/Pow entry); computing that identity exactly is the illegal-states-fewer choice. Clean cases (integer operands, `Mod(5.5, 2)`, `Mod(7, 2.5)`) agree with cue byte-for-byte. | pins `BuiltinTests` `math_mod_exact_beats_cue_float`; agreeing cases in `builtins/math_mod_signbit` |
| Container `==` is type-SENSITIVE on int-vs-float leaves (STRUCT-EQ-LEAF-TYPESENSE, adjudicated 2026-07-04) | v0.16.1 | Equal-magnitude int vs float leaf INSIDE a container: `[1] == [1.0]`, `{a: 1} == {a: 1.0}`, `[[1]] == [[1.0]]` | `false` for all three — `cue` compares container elements type-sensitively (`1` int ≠ `1.0` float), even though its SCALAR `1 == 1.0` is `true` | `true` for all three — the int leaf converts to float and compares equal, recursively at any depth | **The CUE spec MANDATES value-based equality recursively; `cue`'s container `false` is a `cue` bug.** Spec (Comparison operators): "Numeric values are equal if they represent the same number. **When comparing an integer with a floating-point number, the integer is first converted to floating-point.**" plus "List values are equal if they have the same length and their corresponding elements are **recursively equal**" and "Struct values are equal if they have the same set of regular field labels and the corresponding values are **recursively equal**." Recursive element equality reuses `==`, so the int→float carve-out applies inside containers exactly as at the scalar top level. `cue` is internally INCONSISTENT — value-based at scalar (`1 == 1.0` → `true`, spec-correct) but type-sensitive one level down (`[1] == [1.0]` → `false`, spec-violating). Kue is value-based EVERYWHERE (`concreteEq`'s `.prim` leaf reuses the decimal-aware `evalDecimalCompare?`), so it is spec-correct AND internally consistent. `1 & 1.0 = ⊥` (distinct lattice elements under UNIFICATION) does NOT bear on `==` — comparison is not unification (`1 == 2` is a valid `false`), and the spec defines `==` on numbers by represented-value, not by lattice identity. This CLOSES the STRUCT-EQ-LEAF-TYPESENSE question filed by the 2026-07-04 Phase A audit: its tentative "match cue / type-sensitive" lean was wrong (would import cue's bug); the spec is EXPLICIT, not silent (so no `cue-spec-gaps.md` entry). Kue's `1130638` code is correct as-is. | `numeric/equality_expressions` (`scalarIntFloat`/`listIntFloat`/`structIntFloat`/`nestedList`); pins `EvalTests` `eval_eq_scalar_int_float_true` / `eval_eq_list_int_vs_float_true` / `eval_eq_struct_int_vs_float_true` / `eval_eq_nested_list_int_vs_float_true` / `eval_eq_list_int_vs_float_unequal_false` / `eval_ne_list_int_vs_float_false` |
| Marking a disjunct that already carries a default — outer `*` broadens the inner default set (NESTED-DISJ-MARK, adjudicated 2026-07-05; RECLASSIFIED from a designed-deferral spec-gap) | v0.16.1 | A `*` marks a term that is ITSELF a defaulted disjunction, then a later narrowing kills the inner default: `_I: *1 \| 5` ; `out: (*_I \| 9) & >=5` (scalar), and the struct analog `_#I: {a:5}` ; `_#B: {b: string}` ; `_#O: *_#I \| _#B` ; `out: {(*_#O \| {c: int})} & {b: "x"}` (inner default dies by closedness). | `{"out": 5}` (scalar); `{"out": {"b": "x"}}` (struct) — `cue` resolves each to the surviving inner arm as if it were still a default. | `5 \| 9` AMBIGUOUS → export error (scalar); `{"b":"x"} \| {"c":int,"b":"x"}` AMBIGUOUS → export error (struct) — no default survives, so the disjunction stays open. | **The CUE spec's default algebra is EXPLICIT (not silent); `cue` violates its own marking rule M2 — a `cue` bug.** Spec ("Default values") marking rules: `M1: *⟨v⟩ => ⟨v, v⟩` (introduce a default only for an unmarked/undefaulted term), `M2: *⟨v, d⟩ => ⟨v, d⟩` ("keep existing defaults for marked term" — the outer `*` is ABSORBED, it does NOT re-broaden `d` to the whole set `v`), `M3: ⟨v, d⟩ => ⟨v⟩` (strip a default from an unmarked term). The spec's closing note confirms M2: "for any marked disjunction `a`, the expressions `a\|a`, `*a\|a` and `*a\|*a` all resolve to `a`" — so `*a = a` for an already-defaulted `a`. Derivation (scalar): `_I = *1\|5` ⇒ `⟨1\|5, 1⟩`; `*_I` ⇒ M2 ⇒ `⟨1\|5, 1⟩` (default STAYS `1`); `*_I \| 9` ⇒ D1 ⇒ `⟨1\|5\|9, 1⟩`; `& >=5` ⇒ U1 (`⟨v1,d1⟩ & ⟨v2⟩ => ⟨v1&v2, d1&v2⟩`) ⇒ `⟨(1\|5\|9)&≥5, 1&≥5⟩ = ⟨5\|9, ⊥⟩`. Default `⊥` ⇒ no unique default ⇒ AMBIGUOUS. `cue`'s `5` requires the default set to be `1\|5` (both inner arms), i.e. it applies `*⟨v,d⟩ => ⟨v,v⟩` (M1-after-strip broadening) — exactly what M2 forbids and the `*a=a` note contradicts. `cue` broadens at every nesting level (triple-nest `_J: *_I \| 7` ⇒ `cue` still `5`, spec+Kue `5\|7\|9` ambiguous). The unmarked-group control `((*_#I \| _#B) \| {…}) & {…}` is ambiguous on BOTH (M3 strips, no default) — the group-`*` is the discriminator, but where `cue` diverges it is on the wrong side of M2. Kue needs NO change: its eager flatten of a `(.default, .disj nested)` arm IS M2's absorb-the-mark. Supersedes the 2026-06-23 "designed-deferral / 3rd `Mark` state" plan — that plan would have imported `cue`'s bug. | `TwoPassTests` `nested_disj_mark_marked_inner_default_dies_is_ambiguous` / `nested_disj_mark_marked_inner_struct_default_dies_is_ambiguous` / `nested_disj_mark_triple_nest_dies_is_ambiguous` / `nested_disj_mark_dies_inside_struct_field_is_ambiguous` (+ the survives-narrow / no-narrow / unmarked-control matches) |

| `strconv.FormatInt`/`ParseInt` base beyond Go's `2..36` contract (STDLIB-C, 2026-07-10) | v0.16.1 | `strconv.FormatInt(255, 37)`; `strconv.FormatInt(61, 62)`; `strconv.ParseInt("z", 37, 64)` | `"6x"`; `"Z"`; `35` — cue ACCEPTS base up to 62 (and uses `math/big`'s `0-9a-zA-Z` alphabet with uppercase for digit values `36..61`). | `_\|_` (`strconvInvalidBase`) for base outside `2..36` (plus `0` for the parse auto-detect). Bases WITHIN `2..36` agree with cue byte-for-byte. | Go's `strconv` — the package cue's `strconv` mirrors — DOCUMENTS base `2..36` and `panic`s outside it (`FormatInt`) / errors (`ParseInt`); values `10..35` are the digits `a..z`, and there is no 37th digit in the contract. cue's acceptance of `37..62` is an unintended leak of `math/big`'s wider `Text`/`SetString` range (case-sensitive uppercase digits `A..Z`), inconsistent with the `strconv` surface it presents. Kue follows the documented `2..36` contract, so an out-of-contract base is a clean `invalid base` error rather than a silently base-62 result. The spec proper is silent on `strconv` (a builtin-package surface); the tiebreak is the documented Go contract, recorded here and in `cue-spec-gaps.md`. | `Kue/Tests/StrconvTests.lean` `formatint_base_too_high` / `parseint_invalid_base`; success cases in `export/strconv_basic` |
| `time.ParseDuration` exact integer division vs cue's float64 fractional rounding (STDLIB-TIME, 2026-07-11) | v0.16.1 | A fractional-hour duration whose exact nanosecond count lands on cue's float64 rounding boundary: `time.ParseDuration("0.00427738455750h")`. The exact value `427738455750 · 3600000000000 / 10^14 = 15398584407` is an EXACT integer (remainder 0). | `15398584406` — cue computes the fractional contribution as `uint64(float64(f) · (float64(unit)/scale))` in float64 (Go's `leadingFraction`), and the float64 rounding of `f·unit/scale` truncates one nanosecond low. | `15398584407` — Kue computes `⌊frac·unit/10^n⌋` in EXACT integer arithmetic, yielding the true nanosecond count. | A `time.Duration` IS an exact int64 nanosecond count; the fraction `0.00427738455750h` equals `15398584407 ns` exactly (the division is remainder-free), so that integer is the unambiguous correct value. cue's off-by-one is a float64 precision artifact of Go's `ParseDuration` (the comment in Go's source concedes "float64 is needed to be nanosecond accurate for fractions of hours" — it is NOT accurate). The spec is silent on `time` (a stdlib-package surface, `cue-spec-gaps.md`); the exactness-first principle picks the true integer over the float64 approximation. Non-boundary fractional inputs (the audit's 22 cases: `0.3ns`, `1.9ns`, `100.5ms`, `0.0000001s`, …) agree with cue byte-for-byte — the divergence surfaces only where float64's 52-bit mantissa cannot represent `f·unit` exactly (fractional-hour inputs ≥ ~14 fraction digits). | pins `TimeTests` `pd_fractional_hour_exact_beats_cue_float`; agreeing fractional cases throughout `TimeTests` `pd_*` |
| Newline-elided list literals — auto-comma applies inside `[]` too (LIST-SEP, 2026-07-10) | v0.16.1 | Newline between list elements with no explicit comma: `[1\n2]`, and any multiline comma-less list. cue ACCEPTS the identical elision for STRUCTS (`a:1\nb:2` ⇒ ok). | `_\|_` at PARSE — `missing ',' before newline in list literal` (exit 1). cue rejects newline-elision inside `[]` while accepting it inside `{}`. | `[1, 2]` — the newline auto-inserts a comma, exactly as it does between struct declarations. Same-line comma-less `[1 2]` is still a parse error (`missing ',' in list literal`, matching cue) — auto-comma fires only at a line's end. | **The CUE spec's automatic-comma-insertion rule is UNIFORM; cue's list rejection is the bug.** Spec (Lexical analysis → "Commas"): "a comma is automatically inserted into the token stream immediately after a line's final token if that token is [an identifier, keyword, or bottom; a number or string literal, including an interpolation; one of the characters `)`, `]`, `}`, or `?`; an ellipsis `...`]." The rule names NO exclusion for list literals — a number literal ending a line inside `[]` triggers the same auto-comma as anywhere else. cue is internally INCONSISTENT: it honors the rule for struct declarations (`a:1\nb:2` parses) but rejects it inside list literals (`[1\n2]` → `missing ',' before newline`), so its own struct-vs-list split is the tell that the list rejection is an anomaly, not a spec-mandated carve-out. Kue applies the struct-side `fieldSeparator` uniformly in `parseListItems`, so newline-elision parses in both, and same-line adjacency (`[1 2]`, no line break, so no auto-comma) errors in both. | wild `testdata/wild/list-same-line-no-comma` (`[1 2]` → `.expected.err`); `ParseTests` `parse_list_same_line_no_comma_rejected` / `parse_list_newline_elided_elements` / `parse_list_newline_after_bracket` (+ comma/trailing-comma/nested/ellipsis/empty regression pins) |
| `cue export` fabricates a `list.UniqueItems` bottom on ABSTRACT elements (STDLIB-VALIDATORS HIGH-2, 2026-07-11) | v0.16.1 | `[int,int] & list.UniqueItems` — two abstract elements that coincide structurally but are not concrete. | INTERNALLY INCONSISTENT: `cue eval` RETAINS it (`list.UniqueItems & [int, int]`, correct — incomplete), but `cue export` FABRICATES a bottom (`does not satisfy list.UniqueItems: equal values at position 0 and 1`, exit 1). | `incomplete value` (exit 1) — the abstract ints are unmeasured, so uniqueness is UNDECIDED; the residual is retained and re-checked as elements concretize (`[int,int] & [1,2]` ⇒ `{"x":[1,2]}`, unique). | Two abstract elements are NOT a definite duplicate — they can refine to distinct concretes — so eager-bottoming is unsound (it kills the satisfiable `[1,2]` narrowing). `cue export` applies the same unsound eager check; its own `cue eval` retaining the value is the tell. A duplicate is definite only when both elements are GROUND (`[1,1]`, `[{a:1},{a:1}]` — cue+Kue agree, bottom). Kue's `hasGroundDup` gates the bottom on `Value.isGround`. | `testdata/wild/uniqueitems-abstract-incomplete` (standalone → `.expected.err`); `testdata/wild/uniqueitems-abstract-elements` (concretizes → `{"x":[1,2]}`); `FixtureTests` `uniqueitems_abstract_*` |
| Unresolved multi-arm disjunction export rendering: `ambiguous` vs `incomplete` (STDLIB-VALIDATORS HIGH-1, 2026-07-11) | v0.16.1 | `(string & strings.MinRunes(5)) \| "hi"` — a disjunction whose constrained arm is incomplete and whose concrete arm does not satisfy it, so two live non-default arms remain at export. | `incomplete value strings.MinRunes(5) \| "hi"` (exit 1). | `ambiguous value: multiple non-default disjuncts remain` (exit 1). | DISPLAY-ONLY family (same class as the "residual pattern in eval output" / D#1b rows): the VALUE verdict agrees — both refuse to fabricate a concrete result and error at export; neither collapses the value to `"hi"`. Only the error CLASSIFICATION differs — Kue names the precise cause (two live non-default arms cannot be resolved), cue names the arm's incompleteness. The load-bearing correctness fact (the abstract arm SURVIVES rather than being fabricated-pruned to `"hi"`) holds on both. | `testdata/wild/minrunes-disj-arm-fabricated` (`.expected.err` pins the surviving-arm verdict); `FixtureTests` `minrunes_abstract_disj_arm_survives` |

Every slice through `8af9e2f` (comprehensions, dynamic fields + string interpolation,
struct-embedding scope, `strings`/`list` builtins, decimal-lift refactor) oracle-checked
clean against `cue` v0.16.1; the `export -e` entry was the first deliberate disagreement,
surfaced by the `kue export -e` slice.

## Resolved

- **`for` over a concrete non-iterable — RESOLVED (fix-slice (d), 2026-07-02).** Kue formerly
  zero-iterated a non-iterable `for` source (`for x in 5` → `[]`) where cue hard-errors
  (`cannot range over 5 (found int, want list or struct)`); the row flagged it for
  re-adjudication under the E#4 principle. Re-adjudicated: cue is SPEC-CORRECT (the spec mandates
  `for` range over a list or struct), so Kue was the wrong side. `classifyForSource` now makes a
  CONCRETE / decidably-non-iterable source (a scalar `.prim`/carrier, an abstract scalar type
  `.kind`, a string-regex or numeric bound) a type error, while a genuinely-unresolved source
  (`_`, an unresolved ref/disjunction that may still become a list/struct) DEFERS — matching cue's
  verdict on both. The deferred-source *display* delta (`[for x in @0.0 {@1.0}]` vs cue's `[for x
  in y {x}]`) is the pre-existing D#1b display-only family, value verdict identical, so it needs no
  new divergence row.

## Known kue-side divergences (kue is WRONG — bugs to fix)

The table above records cases where **cue** is the buggy side. This section is the inverse:
cases where **kue** diverges from BOTH the spec AND cue — a known leniency/soundness bug kue
carries, tracked here until fixed. Each has a QUEUED plan item.

_None currently open._ The one prior entry — C-style `/* */` block comments accepted, which
CUE's grammar (only `//` line comments) forbids — was fixed in `BLOCK-COMMENT-REJECT`
(2026-07-11): the `Kue/Parse.lean` trivia scanners no longer treat `/* */` as whitespace, so
every position rejects with a `parse error: … unexpected character` (guard: wild fixture
`block-comment-rejected` + `ParseTests` `parse_block_comment_*`).
