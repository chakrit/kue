# RESUME HERE — AUDIT ROUND COMPLETE (2026-06-20); next leader = truncate-primitive

Live START-HERE pointer; supersedes `2026-06-20-resume-audit-complete-test-org-next.md`
(deleted). Authoritative live roadmap:
[`../spec/spec-conformance-audit.md`](../spec/spec-conformance-audit.md)
§ Consolidated fix backlog (ranked spec-conformance fixes) +
[`../spec/plan.md`](../spec/plan.md) (capabilities, non-spec roadmap, audit verdicts).

## Audit state — COMPLETE. Counter = 0. No audit due; next after 2–3 new slices.

Two-phase audit for the batch **test-org `4b25cef` + BI-1 `9bd6927`/`6065380`** is CLOSED:

- **Phase A `5f1c143`** — BI-1 + test-org SOUND (oracle case table faithful to the Unicode
  STANDARD, not a `cue` quirk; pin-count conserved; ASCII regression clean; 1 LOW
  doc-precision + 2 default-mapping pins fixed inline).
- **Phase B `<this commit>`** — architecture HEALTHY; F-CASE-ARCH RULED (both halves); no
  new code findings.

**Counter reset to 0.** Run the next 2–3 implementation slices before the next two-phase
audit.

## Phase B verdict (this round) — the headline + the health check

- **★ F-CASE-ARCH RULED — both halves discharged.**
  - **(a) committed `CaseTable.lean` STAYS** (reproducible / reviewable / offline / no
    build-time `cue` dep for a frozen leaf table). The
    `DO NOT EDIT — generated by scripts/gen-case-table.py` header + generator provenance
    docstring (with regenerate command) already existed — NO inline fix needed.
  - **(b) oracle-as-data-source convention WRITTEN as an ADR:**
    [`../decisions/2026-06-20-oracle-as-data-source.md`](../decisions/2026-06-20-oracle-as-data-source.md).
    Oracle = sound DATA SOURCE for an externally-standardized, `cue`-faithful domain
    (Unicode case table) — verify vs the EXTERNAL standard, record provenance — NEVER a
    correctness GATE for CUE semantics. Cross-linked from `slice-loop.md` spec-authority
    section.
- **Architecture HEALTHY.** `Builtin → CaseTable` is a clean leaf data dependency
  (`CaseTable` imports nothing; single consumer; no cycle; no `Builtin → Eval` edge).
  `Eval.lean` 3645 lines — under the ~4500 watch; **EvalOps** (plan item 2) stays the
  right standing first carve.
- **Test/fixture-regroup STAYS DEFERRED** (judgment: high blast radius via hand-maintained
  `FixturePorts`, low marginal win). Perf-guide CURRENT — no case-lookup row (per-char
  O(log n) binary search, no fuel/eval/meet — categorically cheap; a row would be
  misleading noise).
- Carried-forward rulings INTACT (re-confirmed, do NOT re-litigate): walker-dedup family
  **AD4-1 → A-EN3+DRY-1 → AD2-1** (four distinct mechanisms, post-argocd, gated behind
  correctness; AD4-1 preserves the verified `[_|_]`≠`_|_` list/struct asymmetry);
  **BI-EFF** (effectful-builtin seam — scoped, triggered at the 2nd effectful builtin,
  registry rejected); **AD3-4** ruled out.

## NEXT LEADER (recommendation) → truncate-primitive (plan item 1, HIGH)

Spawn this as the next slice. Rationale: it is the only HIGH item that is BOTH designed
AND immediately actionable, and it is the repo's illegal-states-unrepresentable
reason-to-be — the truncation-bump invariant (a `fuel=0` helper that drops fields MUST
bump `truncCount`) is held by DISCIPLINE across six sites, and the audit-#6 corruption it
prevents ALREADY shipped once latent. The cheap spec-conformance work is mostly drained,
so soundness hardening is the highest-value move now (over the LOW cosmetic items or the
LARGE undesigned numeric subproject).

- **Step 1 (do now, strictly behavior-preserving):** add an `EvalState.truncate`
  combinator fusing bump+return; rewrite all six `fuel=0`-drop sites through it.
  Byte-identical fixtures; localizes the bump. See `plan.md` item 1.
- **Step 2 (only if cheap):** a `withFuel` combinator routing the `fuel=0` dispatch so a
  seventh helper physically cannot skip the bump — attempt only for the four
  top-level-`fuel`-dispatch helpers; STOP at step 1 + a one-line doc invariant if step 2
  exceeds mechanical.

### Other ranked candidates (after truncate-primitive)

- **BI-2-residual** (MED, LARGE) — `math.Sqrt` (IEEE-754 float64 + `NaN`/`Infinity` + Go
  sci-notation formatter) and `math.Pow` neg/fractional exponent (apd 34-digit decimal
  Pow + Infinity). Both BOTTOM honestly today. Needs a Float/decimal-numeric design fork.
  No real app needs it — a deliberate numeric subproject, not a quick slice.
- **SC-3** display-residual (LOW, spec-gap — cosmetic default-collapse).
- **SC-4** (LOW, spec-gap-FIRST), **SC-1b** (closed×closed-pattern, MED).
- **A#6** (`containsBottom` fuel cap, STANDALONE hardening).
- **The 4 spec-gap ratifications** (LOW, doc-ish — could be ONE quick slice).
- **Periodic passes** (non-blocking): plan-hygiene (distill + refresh
  `docs/www/index.html`), the deferred fixture-regroup.

## CANONICAL PATHS (ground-truth — do NOT re-litigate)

- prod9 stress-test targets:
  `/Users/chakrit/Documents/prod9/infra/apps/argocd.cue` (parked) and
  `.../cert-manager.cue` (fully correct, ~30.5s).
- cue oracle: `/Users/chakrit/go/bin/cue` (v0.16.1) — READ-ONLY, cross-check only.
- kue binary: `.lake/build/bin/kue` (or `lake exe kue`).
- **Python note:** a shell wrapper shadows `python3` with a broken `~/.venv`; use
  `/usr/bin/python3` by absolute path for any generator/oracle scripting (the case-table
  generator hardcodes the cue path and reads only the local oracle — no network).

## STANDING CONTEXT (durable; full detail in CLAUDE.md + guides/slice-loop.md)

- Kue autonomy grant in effect (decide/proceed; resolve forks by philosophy; commit/push
  on `main` when attended). **Spec is authority; `cue` is a fallible cross-check, never
  the gate** — EXCEPT the one narrow oracle-as-data-source carve now governed by the ADR
  (data, never a gate). Correctness-over-performance. **Unattended/AFK → commit, don't
  push** (CLAUDE.md).
- Orchestrator = thin re-spawner; one subagent per slice; two-phase audit (A then B) every
  2–3 slices. Per-slice duties: tests-first; log `cue-divergences.md`; flag
  `cue-spec-gaps.md`; keep `kue-performance.md` current.
- prod9 + cue caches READ-ONLY. NO `git checkout`/`restore`/`reset --hard` on the main
  tree.
- **argocd / Bug2-5: PARKED** — a stress-test finding, not on the critical path.
