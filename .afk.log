# AFK run — 2026-07-03/04 (supersedes all prior summaries)

Gate GREEN (`./scripts/check.sh`), tree clean, everything committed on `main`, **NOT pushed**
(AFK envelope). ~47 commits unpushed. Every commit is canary-byte-identical (cert-manager
`kue export` == `cue export`, jq -S) and audited.

## Landed this run (chronological themes)

- **Your infra asks:** 2-core CPU cap + permanent `nice` via `./lake`/`./lean` wrappers
  (build ONLY via `./lake`); Lean toolchain **v4.29.1 → v4.31.0** (clean, canary identical);
  no external deps to bump.
- **Protocol amendments A1–A8** applied + the operational retro (failure-modes recorded,
  conventions hardened: explicit-pathspec commits, long-builds-are-the-orchestrator's-job).
- **Correctness campaign — the captured backlog is CLOSED, all red seeds graduated except
  two architectural quarantines (byte-literal-high-byte, byte-literal-interpolation):**
  L5 closedness (3 seeds); per-file import scoping; `let`/alias no-shadow (forward+reverse,
  via a new `Field.quoted` model); a quoted-BEq equality regression (found by audit, fixed);
  concrete struct/list `==`; float-by-value unification (`1.0 & 1.00`); `strings.Runes`;
  list slicing `x[lo:hi]`; interpolation operand typing; byte-literal escape decoding.
- **Conformance probes** (the North-Star bug-hunt): numeric → found+fixed a float
  self-inconsistency bug; interpolation/regexp/encoding → found+fixed interp-typing;
  disjunctions/defaults → CLEAN (value-conformant, guard theorems added).
- **int/float `==` spec adjudication:** ruled kue CORRECT / cue BUGGY (`[1]==[1.0]` is
  spec-true; cue's container `false` is a cue bug) — recorded in cue-divergences.
- **Audits:** THREE full two-phase (A+B) rounds this session, all closed HEALTHY (findings
  fixed or filed). Latest Phase B judges the core CUE semantic surface **substantially
  complete** — frontier shifted from correctness to representation-quality + non-core.

## OPEN (for you)

1. **`git push` the ~47 commits** + cut the owed alpha (both PUSH-class, AFK-blocked).
2. **Highest-leverage next slice (attended-grade):** **BYTE-ARRAY-REPR (0f)** — bytes are
   String-backed (`Prim.bytes : String`) so can't hold an octet ≥0x80; a `Prim.bytes :
   Array UInt8` refactor (MEDIUM, ~16 sites/9 files) fixes 3 latent bugs and unblocks
   BYTE-INTERPOLATION + BYTES-SLICE. Recommended `Array UInt8` (keeps derived BEq/Repr).
3. **Autonomously-safe but deliberate (churny core-type changes; this run showed such
   changes carry regression risk):** ARCH-QUOTED-STRIP (0c), PRIM-FLOAT-PARSED (0e).
4. **Broad:** BUILTIN-IMPORT-LENIENCY (kue resolves stdlib builtins without the `import`
   cue requires — enforcing likely needs a fixture-corpus migration).
5. **Designed-deferral / blocked:** NESTED-DISJ-MARK (tier-2 divergence), STRUCT-EQ half-2
   (dedup order-independence, touches global equality), B3d-6b (network-gated).

Detail + restore point: the breadcrumb under `docs/notes/` (kept current per slice) and
`docs/spec/plan.md` (ranked backlog). Operational lessons: `docs/reference/failure-modes.md`.

## State read
Not stopped for lack of tasks — the autonomous *correctness* well is worked out (an
independent audit confirms it). Remaining is representation-quality / attended / broad /
blocked. The loop continues on the lowest-risk high-value option (more conformance
probing) rather than churning a risky core-type refactor unattended.
